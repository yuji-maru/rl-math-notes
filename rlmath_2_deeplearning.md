---
tags:
  - 強化学習
  - 深層学習
  - 理論
aliases:
  - 深層RL理論編
author: Claude Fable 5 (high)
---
# 深層強化学習の数理 — Bellman方程式から現代的アルゴリズムへ

> [!note] シリーズ全体の目次
> - [[強化学習の数理_1_基礎編]] — 古典的RL理論(MDP・Bellman方程式・DP・TD・方策勾配)
> - **[[強化学習の数理_2_深層学習編]]**(本稿) — 深層強化学習(DQN・A2C・PPO・SAC等)
> - [[強化学習の数理_3_演習_手計算編]] — 上記2冊の導出を手計算で再構成する演習
> - [[強化学習の数理_4_演習_実践コーディング編]] — 上記2冊のアルゴリズムをコードで実装する演習

> [!abstract] 本テキストについて
> 本テキストは、前作『強化学習の数理的基礎 — MDPとBellman方程式から体系的に学ぶ』の**続編**であり、深層ニューラルネットワークを関数近似器として用いる**深層強化学習 (Deep Reinforcement Learning)** の主要アルゴリズムを、理論的な導出を軸に体系的に解説する。対象読者は前作と同じく物理学修士レベルの数学的素養をもち、かつ**前作の内容(MDP、Bellman方程式、縮小写像、GPI、TD学習、Q学習、方策勾配定理、actor-critic)を習得済み**の者を想定する。
>
> 本テキストの一貫した視点は次の通りである:**深層強化学習の各手法は、前作で確立した理論(その多くは厳密な収束保証をもつ)を、保証が崩れる領域——非線形関数近似——へ外挿する際に生じる病理と、それに対する処方箋の体系である。** 新奇なアルゴリズムの羅列としてではなく、「前作のどの定理のどの仮定が破れ、それをどう補修しているか」を常に明示しながら進む。

---

## 目次

- **第0章** 序論 — 表形式の世界から深層の世界へ
- **第1章** 準備:関数近似器としての深層ニューラルネットワーク
- **第2章** DQN — 深層Q学習と不安定性への最初の処方箋
- **第3章** DQNの系譜 — Double DQN・Dueling・優先度付き経験再生・分布型RL・Rainbow
- **第4章** 深層方策勾配 (I) — A2C/A3C と一般化アドバンテージ推定 (GAE)
- **第5章** 深層方策勾配 (II) — 性能差分補題・自然勾配・TRPO・PPO
- **第6章** 連続行動制御 — 決定論的方策勾配・DDPG・TD3
- **第7章** 最大エントロピー強化学習 — ソフトBellman方程式とSAC
- **第8章** モデルベースRLとプランニング — MCTS・AlphaZero・MuZero・世界モデル
- **第9章** 現代的フロンティア — オフラインRL・探索・RLHF
- **第10章** 総括 — 全体を貫く設計原理
- **付録A** Fisher情報行列と自然勾配の導出
- **付録B** 再パラメータ化勾配とスコア関数勾配の比較
- **付録C** 記法一覧(前作からの追加分)

---

# 第0章 序論 — 表形式の世界から深層の世界へ

## 0.1 前作の到達点と、残された問題

前作で我々は、次の理論的成果を手にした:

1. 価値関数はBellman方程式の解として特徴づけられ、Bellman作用素の $\gamma$-縮小性により、**表形式**の反復法(価値反復、Q学習など)は真値への収束が**証明**できる。
2. 一般化方策反復 (GPI) ——「評価」と「改善」の交互反復——がすべての制御アルゴリズムの共通骨格である。
3. 方策勾配定理により、方策を直接パラメータ化して期待収益の勾配を不偏推定できる。

一方、前作の末尾で明らかにした通り、状態空間が巨大な現実の問題(画像入力、ロボット制御、囲碁)では表は持てず、**関数近似**が不可避である。そして前作第9章で警告した通り、そこには **deadly triad(関数近似+ブートストラップ+方策オフ学習)** という地雷が埋まっている。

## 0.2 なぜ「深層」なのか

線形近似 $\hat v(s;\boldsymbol w) = \boldsymbol w^\top \boldsymbol\phi(s)$ には(方策オン設定なら)収束理論があった。ではなぜ、理論保証を捨ててまで深層ニューラルネットワーク (DNN) に進むのか。答えは**特徴量 $\boldsymbol\phi$ の設計問題**にある。線形近似の性能は特徴量の質で決まるが、生のカメラ画像から「良い特徴量」を人手で設計することは絶望的に難しい。DNNは**特徴抽出そのものをデータから学習する**——畳み込み層が画像から空間的特徴の階層を、全結合層がそれらの非線形な組み合わせを自動的に構成する。2013–2015年のDQNの衝撃は、まさに「生ピクセルから、人手の特徴設計なしに、多数のAtariゲームを人間レベルでプレイする単一のアーキテクチャ」を示した点にあった。

> [!important] 本テキストの中心的緊張関係
> 深層強化学習とは、次のトレードオフの上に立つ分野である:
> - **得るもの:** 表現力と汎化。生の高次元観測から価値・方策を end-to-end で学習できる。
> - **失うもの:** 収束保証。縮小写像の議論(前作第5章)は、更新が「各状態の値を独立に動かせる」表形式性、あるいは射影の非拡大性(線形・方策オン)に依存していた。DNNではどちらも成り立たない。
>
> したがって本テキストに登場する無数の工夫——経験再生、ターゲットネットワーク、クリッピング、信頼領域、二重推定、エントロピー正則化——は、すべて**失われた安定性を実用上取り戻すための補修**である。各章で「これは前作のどの理論のどの破れへの処方箋か」を明示するので、その対応を意識して読んでほしい。

## 0.3 本テキストの見取り図

深層強化学習の主要アルゴリズムは、前作で築いた2本の柱に沿って系統樹をなす:

```
前作の理論
 ├─ 価値ベースの柱 (Q学習)
 │    └─ 第2章 DQN ─ 第3章 Double/Dueling/PER/分布型/Rainbow
 │
 ├─ 方策ベースの柱 (方策勾配・actor-critic)
 │    ├─ 第4章 A2C/A3C + GAE
 │    ├─ 第5章 TRPO → PPO      (確率的方策・信頼領域)
 │    ├─ 第6章 DPG → DDPG → TD3 (決定論的方策・連続行動)
 │    └─ 第7章 SAC              (最大エントロピー・両柱の統合)
 │
 └─ モデル・プランニングの柱 (動的計画法)
      └─ 第8章 MCTS / AlphaZero / MuZero / 世界モデル
```

第9章で、これらの外側に広がる現代的フロンティア(オフラインRL、探索、RLHF)を展望する。

---

# 第1章 準備:関数近似器としての深層ニューラルネットワーク

本章では、以降で必要になる深層学習の知識を、強化学習の文脈に必要な範囲に絞って整理する。深層学習自体の体系的解説は他書に譲る。

## 1.1 ニューラルネットワークとは何か:パラメトリックな関数族

$L$ 層の全結合ネットワークは、アフィン変換と成分ごとの非線形関数(活性化関数)$\sigma$ の交互合成である:

$$
f_{\boldsymbol\theta}(\boldsymbol x) = W_L\, \sigma\!\left( W_{L-1}\, \sigma\!\left( \cdots \sigma(W_1 \boldsymbol x + \boldsymbol b_1) \cdots \right) + \boldsymbol b_{L-1} \right) + \boldsymbol b_L
$$

パラメータ $\boldsymbol\theta = \{W_\ell, \boldsymbol b_\ell\}$ は数百万〜数十億次元に及ぶ。普遍近似定理により、十分広い(あるいは深い)ネットワークはコンパクト集合上の任意の連続関数を任意精度で近似できる。強化学習の観点で重要なのは次の3点である:

1. **微分可能性:** $\nabla_{\boldsymbol\theta} f_{\boldsymbol\theta}(\boldsymbol x)$ が誤差逆伝播法(連鎖律の効率的実装、計算量は順伝播と同オーダー)で計算できる。前作の semi-gradient 法や方策勾配法は $\nabla_{\boldsymbol\theta} \hat v$ や $\nabla_{\boldsymbol\theta} \ln \pi$ さえ計算できれば形式上そのまま動くので、**アルゴリズムの骨格は前作から変わらない**。
2. **大域的な結合(汎化と干渉の表裏一体):** 1つのサンプルによる更新が、パラメータを介して**すべての入力に対する出力を動かす**。これが汎化の源泉であると同時に、学習済みの知識が新しい更新で破壊される**破滅的忘却**や、後述する自己言及的発散の温床になる。表形式($\boldsymbol\phi(s) = \boldsymbol e_s$、更新が完全に局所的)との最大の違いはここにある。
3. **入力構造に応じた設計:** 画像には畳み込みネットワーク (CNN)、系列・部分観測には再帰型ネットワークやTransformerが使われる。以降、アーキテクチャの詳細には立ち入らず、「微分可能なパラメトリック関数族 $f_{\boldsymbol\theta}$」として抽象的に扱う。

## 1.2 確率的勾配降下法と、教師あり学習との決定的な違い

損失 $\mathcal L(\boldsymbol\theta) = \mathbb E_{\xi \sim \mathcal D}[\ell(\boldsymbol\theta; \xi)]$ をミニバッチ勾配で最小化するのが確率的勾配降下法 (SGD) であり、実務では慣性項と座標ごとの学習率適応を組み合わせたAdam等の変種が使われる。教師あり学習でSGDがうまく働く背景には、次の前提がある:

- **(i) データ分布 $\mathcal D$ が固定**されている。
- **(ii) サンプルが独立同分布 (i.i.d.)** に引ける。
- **(iii) 教師信号(ラベル)が固定**されており、パラメータに依存しない。

> [!warning] 強化学習ではこの3つが全滅する
> - (i) データ分布は方策が生成する。方策は学習中に変わり続けるので、**分布は非定常**である(方策勾配法では特に深刻。第5章)。
> - (ii) 軌道上の連続したサンプル $(s_t, a_t, r_{t+1}, s_{t+1})$ は時間的に**強く相関**している。
> - (iii) TD学習の教師信号(ブートストラップ目標)$r + \gamma \max_{a'} Q_{\boldsymbol\theta}(s', a')$ は**学習中のパラメータ自身に依存**する(前作9.2節のsemi-gradient問題)。
>
> 深層強化学習のアルゴリズム設計とは、大部分において、**この3つの破れをいかに教師あり学習に近い状況へ引き戻すか**の技術である。この観点を持つと、次章のDQNの2大発明(経験再生・ターゲットネットワーク)の意味が一目で見える。

## 1.3 損失関数の言葉への翻訳

前作の更新則を、深層学習の標準的な道具(自動微分+オプティマイザ)に載せるには、「更新則」を「損失関数の勾配」として書き直すのが便利である。例えばTD(0)のsemi-gradient更新は、**目標値 $y$ を定数として扱った**二乗損失

$$
\mathcal L(\boldsymbol\theta) = \frac12 \left( y - \hat v(s; \boldsymbol\theta) \right)^2,
\qquad y = r + \gamma\, \hat v(s'; \bar{\boldsymbol\theta}) \quad (\bar{\boldsymbol\theta} \text{は勾配を流さない})
$$

の勾配降下と等価である。「勾配を流さない(stop-gradient)」という操作がsemi-gradientの実装上の正体であり、以降すべての価値学習で暗黙に使われる。

---

# 第2章 DQN — 深層Q学習と不安定性への最初の処方箋

## 2.1 素朴な深層Q学習と、その破綻

前作8.5節のQ学習を、そのままDNNに載せてみよう。Q関数を $Q_{\boldsymbol\theta}(s, a)$($|\mathcal A|$ 個の出力ヘッドをもつネットワーク:状態を入れると全行動のQ値が一度に出る)で表し、遷移 $(s, a, r, s')$ を観測するたびに損失

$$
\mathcal L(\boldsymbol\theta) = \frac12 \left( \underbrace{r + \gamma \max_{a'} Q_{\boldsymbol\theta}(s', a')}_{\text{目標 } y} - Q_{\boldsymbol\theta}(s, a) \right)^2
$$

を(目標を定数扱いして)1ステップSGDする。これは前作の理論の直訳だが、**実際にやるとほぼ確実に発散するか、性能が激しく振動する**。原因は1.2節の3つの破れが同時に襲うためであり、具体的には:

1. **相関したデータ:** 軌道に沿ったオンライン更新では、直近の類似サンプルばかりで学習し、ネットワークが局所的な経験に過剰適合 → 方策が変わって別の状態域に入ると以前の学習が破壊される、を繰り返す。
2. **動く目標との追いかけっこ:** 目標 $y$ が $\boldsymbol\theta$ に依存するため、$Q_{\boldsymbol\theta}(s,a)$ を $y$ に近づける更新が、汎化を通じて $Q_{\boldsymbol\theta}(s', a')$ も動かし、目標が逃げる。正のフィードバックがかかると値が発散する。これは**deadly triad(前作9.4節)の非線形版が現実に発火した姿**である。

## 2.2 DQNの2大発明

Mnihら (2013, 2015) のDQNは、この2つの病理にそれぞれ対応する処方箋を導入した。

**(1) 経験再生 (Experience Replay)。** 遷移 $(s, a, r, s')$ を**リプレイバッファ** $\mathcal D$(容量 $N \sim 10^6$ の循環バッファ)に蓄積し、学習時はバッファから**一様ランダムにミニバッチをサンプル**して更新する。効果は:

- サンプル間の時間相関が破壊され、i.i.d.に近い状況が回復する(1.2節(ii)への処方箋)。
- 各経験が繰り返し使われ、サンプル効率が上がる。
- バッファには過去の(いまとは異なる)方策が生成したデータが混在する。これが許されるのは、**Q学習が方策オフ型である**(前作8.5節:目標方策=貪欲方策と挙動方策の分離)からである。SARSAのような方策オン型アルゴリズムでは経験再生は正当化されない。前作で方策オン/オフの区別を強調した理由がここで回収される。

**(2) ターゲットネットワーク (Target Network)。** 目標値の計算に、**別のパラメータ $\boldsymbol\theta^-$ をもつネットワークのコピー**を使う:

$$
y = r + \gamma \max_{a'} Q_{\boldsymbol\theta^-}(s', a')
$$

$\boldsymbol\theta^-$ は $C$ ステップ(例:$10^4$)ごとに $\boldsymbol\theta^- \leftarrow \boldsymbol\theta$ と同期する(あるいは後の手法では毎ステップ $\boldsymbol\theta^- \leftarrow \tau \boldsymbol\theta + (1-\tau)\boldsymbol\theta^-$、$\tau \ll 1$ とゆっくり追従させる:**Polyak平均**)。目標が一定期間**凍結**されるため、追いかけっこの自己言及ループが切断され、その期間中は「固定ラベルへの回帰」という教師あり学習の問題(1.2節(iii)への処方箋)に近づく。

> [!tip] 直観:ターゲットネットワークは「反復的方策評価の外側ループ」の復活
> 前作第6章の反復的方策評価を思い出そう。厳密なDPでは $v_{k+1} = T^\pi v_k$ と、**古い価値 $v_k$ を固定して**新しい価値を計算した。素朴な深層Q学習は、この「$v_k$ の固定」を捨てて自分の最新値を目標にしてしまっていた。ターゲットネットワークは、外側の反復インデックス $k$(=凍結期間)を復元し、各期間内で「$T^* Q_{\boldsymbol\theta^-}$ への回帰」を近似的に解いている、と読める。つまり**DQNは、Bellman最適作用素の反復 $Q_{k+1} \approx T^* Q_k$ を、各ステップを教師あり回帰で解きながら実行するアルゴリズム**である。この描像は Fitted Q-Iteration と呼ばれる古典的枠組みと一致し、DQNを最も見通しよく理解する視点である。

## 2.3 DQNアルゴリズムの全体像

> [!note] アルゴリズム 2.1(DQN)
> 初期化:Qネットワーク $\boldsymbol\theta$、ターゲット $\boldsymbol\theta^- \leftarrow \boldsymbol\theta$、空のリプレイバッファ $\mathcal D$。
> 各環境ステップで:
> 1. $\varepsilon$-貪欲で行動選択:$a_t = \arg\max_a Q_{\boldsymbol\theta}(s_t, a)$(確率 $1-\varepsilon$)/ランダム(確率 $\varepsilon$)。$\varepsilon$ は初期1.0から徐々に減衰(前作7.3節のGLIEの実用版)。
> 2. 遷移 $(s_t, a_t, r_{t+1}, s_{t+1})$ を $\mathcal D$ に格納。
> 3. $\mathcal D$ からミニバッチ $\{(s_i, a_i, r_i, s_i')\}$ を一様サンプルし、損失
> $$
> \mathcal L(\boldsymbol\theta) = \frac1B \sum_i \left( r_i + \gamma \max_{a'} Q_{\boldsymbol\theta^-}(s_i', a') - Q_{\boldsymbol\theta}(s_i, a_i) \right)^2
> $$
> を1ステップ勾配降下(終端遷移では目標を $r_i$ のみとする)。
> 4. $C$ ステップごとに $\boldsymbol\theta^- \leftarrow \boldsymbol\theta$。

補助的だが効果の大きい工夫として、報酬のクリッピング(Atariでは $[-1,1]$ に制限。ゲーム間でスケールが桁違いに異なる報酬に単一のハイパーパラメータで対処するため)、フレームのスタック(直近4フレームを重ねて入力し、速度情報を含む近似マルコフ状態を構成する——前作1.2節「状態の定義をリッチにしてマルコフ化する」の実例)、TD誤差に対するHuber損失(外れ値への頑健化)がある。

## 2.4 何が示され、何が示されていないか

DQNはAtari 2600の49ゲームで、同一のネットワーク・同一のハイパーパラメータのまま、多くのゲームで人間レベル以上に達した。これは「生の高次元観測からの end-to-end 強化学習」が可能であることの最初の大規模な実証だった。

一方、理論的には、DQNの収束は一般には証明されていない。deadly triadは緩和されただけで消えてはおらず、実際、発散こそ稀になったものの、Q値の系統的な**過大評価**という(前作8.5節で予告した)別の病理が残っている。これが次章の出発点である。

---

# 第3章 DQNの系譜 — Rainbowに至る改良の解剖

本章では、DQNの弱点を一つずつ特定し修復していった一連の研究を扱う。それぞれが前作のどの理論的論点に対応するかを明示する。

## 3.1 Double DQN — 過大評価バイアスの外科手術

**病理の再確認。** 前作8.5節で示した通り、目標値中の $\max_{a'} Q(s', a')$ は、推定ノイズの存在下で系統的な過大評価を生む。もう一歩定量的に見よう。各行動の推定値を $Q(s',a') = Q^{\text{true}}(s',a') + \epsilon_{a'}$($\epsilon_{a'}$ は平均0のノイズ)とすると、

$$
\mathbb E\left[ \max_{a'} Q(s', a') \right] \ge \max_{a'} \mathbb E\left[ Q(s', a') \right] = \max_{a'} Q^{\text{true}}(s', a')
$$

(最大値をとる操作は凸なのでJensenの不等式)。等号は行動が事実上1つのときに限られ、**行動数が多いほど、ノイズが大きいほどバイアスは増大**する。真値がすべて等しく $\epsilon_{a'} \sim \mathcal N(0, \sigma^2)$ i.i.d. なら、バイアスは極値統計により $\sigma\sqrt{2 \ln |\mathcal A|}$ 程度になる。さらに悪いことに、この過大評価はブートストラップにより**次の目標値に伝播して累積**する。

**バイアスの分解と処方箋。** $\max_{a'} Q(s', a')$ は、実は2つの操作の合成である:

$$
\max_{a'} Q(s', a') = Q\big(s',\ \underbrace{\arg\max_{a'} Q(s', a')}_{\text{選択}}\big) \quad \leftarrow \text{評価}
$$

**同じノイズを含む推定量で「選択」と「評価」の両方を行う**ため、ノイズが正に出た行動が選ばれ、その正のノイズがそのまま評価に乗る——これがバイアスの機構である。ならば、選択と評価を**独立な(相関の弱い)2つの推定量に分担させれば**、選ばれた行動のノイズは評価側では平均0となり、バイアスが消える(あるいは大幅に減る)。これが前作で触れたDouble Q学習の思想であり、DQNには次の形で移植された:

> [!note] Double DQN (van Hasselt et al., 2016)
> DQNには幸い、すでに2つのネットワーク($\boldsymbol\theta$ と $\boldsymbol\theta^-$)がある。**選択をオンラインネットワーク、評価をターゲットネットワーク**に割り当てる:
> $$
> y^{\text{DDQN}} = r + \gamma\, Q_{\boldsymbol\theta^-}\!\left( s',\ \arg\max_{a'} Q_{\boldsymbol\theta}(s', a') \right)
> $$

変更はこの1行だけだが、Atariの多くのゲームでQ値の推定が真値に近づき、性能も改善した。「推定量の統計的性質(バイアス)を解析し、最小の変更で修復する」という、理論が実装に直結した美しい例である。

## 3.2 Dueling Network — 価値とアドバンテージの分解

前作2.3節で定義したアドバンテージ分解 $Q^\pi(s,a) = V^\pi(s) + A^\pi(s,a)$ を、**ネットワークのアーキテクチャに埋め込む**のがDueling Network (Wang et al., 2016) である。共有の特徴抽出層の後、スカラー $V_{\boldsymbol\theta}(s)$ を出すヘッドとベクトル $A_{\boldsymbol\theta}(s, a)$ を出すヘッドに分岐し、

$$
Q_{\boldsymbol\theta}(s, a) = V_{\boldsymbol\theta}(s) + \left( A_{\boldsymbol\theta}(s, a) - \frac{1}{|\mathcal A|} \sum_{a'} A_{\boldsymbol\theta}(s, a') \right)
$$

と合成する。括弧内の平均の差し引きは**同定可能性**のためである:分解 $Q = V + A$ には $V \to V + c$, $A \to A - c$ という任意性(ゲージ自由度)があり、これを放置すると2つのヘッドの役割が定まらない。平均をゼロに固定する条件($\sum_a A = 0$。前作2.3節で見た $\pi$ 平均ゼロの性質の一様方策版)がゲージ固定として働く。

**なぜ効くのか。** 多くの状態では、どの行動をとってもさほど結果が変わらない(例:障害物のない直線路)。そのような状態では $Q(s,a)$ の $a$ 依存性は小さく、値の大半は $V(s)$ が担う。分解により、**1回の更新で $V$ ヘッドが全行動のQ値を同時に学習**でき(状態の価値の学習が行動をまたいで共有される)、さらに $Q$ 値の僅差($A$ の差)が $V$ の大きなスケールに埋もれずに表現できる。

## 3.3 優先度付き経験再生 (Prioritized Experience Replay)

一様サンプリングは、「学べることの多い経験」も「もう学び尽くした経験」も等確率で選ぶ。学習の進み具合の自然な指標は**TD誤差の大きさ** $|\delta_i|$(Bellman方程式の残差の1サンプル版:前作8.1節)であり、これが大きい遷移を優先的に再生するのがPER (Schaul et al., 2016) である:

$$
p_i \propto \left( |\delta_i| + \epsilon \right)^{\alpha}
$$

($\alpha$ で優先度の強さを調節、$\alpha = 0$ で一様に帰着。)

> [!warning] 分布を歪めた代償は重点サンプリングで払う
> 非一様サンプリングは、更新の期待値がもはや意図した損失の勾配でなくなるというバイアスを生む。前作7.4節の重点サンプリングの出番である:サンプル $i$ の更新に重み
> $$
> w_i = \left( \frac{1}{N}\cdot\frac{1}{p_i} \right)^{\beta}
> $$
> を乗じて補正する($N$ はバッファサイズ)。$\beta = 1$ で完全補正。実用上は学習初期の $\beta < 1$ から終盤 $\beta \to 1$ へアニールする(初期はバイアスより学習速度を優先し、収束が問題になる終盤に不偏性を回復する)。「分布を都合よく変えたら尤度比で補正する」という統計物理の再重み付けと同じ規律が、ここでも貫かれている。

## 3.4 分布型強化学習 (Distributional RL) — 期待値から分布へ

ここまで価値関数は収益の**期待値** $Q(s,a) = \mathbb E[G_t | s, a]$ だった。分布型RL (Bellemare et al., 2017) は、**収益の確率分布そのもの**を学習対象にする。収益を確率変数 $Z(s,a)$($\mathbb E[Z] = Q$)とすると、前作の収益の再帰式 $G_t = R_{t+1} + \gamma G_{t+1}$ は分布の等式として

$$
Z(s, a) \overset{D}{=} R + \gamma\, Z(S', A'), \qquad S' \sim P(\cdot|s,a),\ A' \sim \pi(\cdot|S')
$$

と持ち上がる($\overset{D}{=}$ は分布の一致)。これを**分布型Bellman方程式**と呼ぶ。右辺の操作(次状態の収益分布を $\gamma$ 倍に縮め、$R$ だけ平行移動し、遷移で混合する)を分布空間上の作用素とみなすと、方策評価版はWasserstein距離のもとで $\gamma$-縮小になることが証明できる——**前作第5章の縮小写像の理論が、関数空間から分布の空間へそのまま昇格する**のである(制御版 $T^*$ については縮小性が一般に成り立たない点に理論上の注意がある)。

実装の代表例 **C51** は、分布を固定した51個のアトム(サポート点)上のカテゴリカル分布 $Z_{\boldsymbol\theta}(s,a) = \sum_j p_j(s,a)\, \delta_{z_j}$ で表現し、Bellman更新後の分布(アトムが $r + \gamma z_j$ にずれる)を元のサポートに射影してから交差エントロピーで回帰する。後継の **QR-DQN** は逆に確率を等分固定して分位点の位置を学習する(分位点回帰)。

**なぜ期待値だけでなく分布を学ぶと性能が上がるのか。** 制御に最終的に使うのは平均だけなのだから一見無駄に思えるが、(i) 分布という豊かな教師信号が表現学習を助ける(同じ平均でも形の違う分布を区別する特徴が育つ)、(ii) 平均の回帰よりも学習信号が安定する、と考えられている。物理の直観でいえば、基底状態エネルギー(平均)だけを合わせるより、スペクトル全体(分布)を合わせる方が、系の記述(特徴表現)が良く拘束される、という感覚である。またリスク感応的な制御(分散やCVaRを考慮した意思決定)への自然な拡張点にもなる。

## 3.5 その他の部品と Rainbow

- **多ステップ学習:** 目標を $n$ ステップ収益 $\sum_{k=0}^{n-1}\gamma^k r_{t+k+1} + \gamma^n \max_{a'} Q_{\boldsymbol\theta^-}(s_{t+n}, a')$ にする。前作8.2節のバイアス・バリアンス調節そのもの(実報酬の割合を増やしてブートストラップのバイアスを薄める)。ただしバッファ内の古いデータに対する $n$ ステップ目標は厳密には方策オフ補正が必要で、それを省略している点は理論的妥協である。
- **Noisy Networks:** $\varepsilon$-貪欲の代わりに、ネットワークの重み自体に学習可能なノイズ $W = \mu + \sigma \odot \varepsilon$ を入れ、探索の強さを状態依存で自動調節する。
- **Rainbow (Hessel et al., 2018):** 上記6要素(Double、Dueling、PER、多ステップ、分布型、Noisy)をDQNにすべて統合したもの。アブレーション実験により、各部品の寄与(特にPER・多ステップ・分布型が大きい)が定量化された。**独立に動機づけられた理論的修復が、加法的に近い形で積み上がる**ことを示した点で、この分野の工学的成熟を象徴する仕事である。

> [!important] 価値ベースの柱のまとめ
> DQN系列は一貫して「**Bellman最適方程式の残差を、教師あり回帰の形でサンプルから最小化する**」枠組みの中にあり、改良はすべて (a) 推定量の統計的性質の修復(Double、分布型)、(b) データ分布の制御(経験再生、PER)、(c) 目標の安定化(ターゲットネット、多ステップ)、(d) 帰納バイアスの注入(Dueling)に分類できる。一方でこの枠組みは $\max_{a'}$ の計算を要するため、**行動空間が連続だと直接は使えない**。その制約を突破するのが方策ベースの柱(第4–7章)である。

---

# 第4章 深層方策勾配 (I) — A2C/A3C と一般化アドバンテージ推定 (GAE)

## 4.1 出発点:前作のactor-criticの復習と深層化

前作第10章の到達点を思い出す。方策勾配定理

$$
\nabla_{\boldsymbol\theta} J(\boldsymbol\theta) \propto \mathbb E_{s \sim d^\pi,\ a \sim \pi_{\boldsymbol\theta}} \left[ \nabla_{\boldsymbol\theta} \ln \pi_{\boldsymbol\theta}(a|s)\; \Psi(s,a) \right]
\tag{4.1}
$$

において、重み $\Psi$ には収益 $G_t$(REINFORCE)、$Q^\pi$、アドバンテージ $A^\pi$、TD誤差 $\delta_t$ など、期待値が(近似的に)アドバンテージに一致する様々な量が使え、選択は**バイアスと分散のトレードオフ**を決めるのだった。

深層化は形式上は単純である:方策 $\pi_{\boldsymbol\theta}$(離散行動ならsoftmax出力、連続行動なら平均・分散を出力するガウス方策)とクリティック $\hat V_{\boldsymbol w}$ をそれぞれDNNにし、

$$
\text{アクター損失: } -\ln \pi_{\boldsymbol\theta}(a_t|s_t)\, \hat A_t, \qquad
\text{クリティック損失: } \frac12\left( \hat R_t - \hat V_{\boldsymbol w}(s_t) \right)^2
$$

を勾配法で最適化する($\hat A_t$ はアドバンテージ推定量、$\hat R_t$ は収益目標。両者の設計が本章の主題)。しかし実際に安定して動かすには、方策勾配特有の2つの問題への対処が要る:

1. **勾配推定の分散**(前作から続く宿題)。
2. **データの非定常性・相関**。方策勾配は(4.1)の期待値が「現在の方策の分布 $d^\pi, \pi$」でとられているため本質的に**方策オン型**であり、DQNのように古いデータをリプレイバッファから再利用することが(補正なしには)できない。相関を切る別の方法が要る。

## 4.2 A3C / A2C — 並列化によるデータ脱相関

Mnihら (2016) の**A3C** (Asynchronous Advantage Actor-Critic) の答えは:**多数の環境コピーを並列に走らせる**。数十のワーカーがそれぞれ独立に環境と相互作用すれば、同時刻に集まるサンプルは(異なる環境状態にいるため)互いにほぼ独立であり、経験再生なしで脱相関が達成できる。方策オン性を保ったままDQNの利点(i.i.d.化)を得る、対照的な設計である。A3Cは非同期にパラメータを更新するが、後に同期版の**A2C**(全ワーカーのバッチをまとめて1回更新)が、GPU効率と再現性の面で標準となった。

もう一つの重要な部品が**エントロピー正則化**である。目的関数に方策のエントロピーを加える:

$$
J_{\text{ent}}(\boldsymbol\theta) = J(\boldsymbol\theta) + \beta\, \mathbb E_{s}\left[ \mathcal H(\pi_{\boldsymbol\theta}(\cdot|s)) \right],
\qquad \mathcal H(\pi(\cdot|s)) = -\sum_a \pi(a|s) \ln \pi(a|s)
$$

これは方策が早期に決定論化(確率が1点に凝縮)して探索が死ぬのを防ぐ。$\beta$ は温度の役割を果たし、前作7.3節のBoltzmann方策・焼きなましの直観がそのまま通用する。この項を「小手先の正則化」ではなく**目的関数の原理的な変更**として真剣に受け取ると、第7章の最大エントロピーRLに到達する——ここは伏線である。

## 4.3 一般化アドバンテージ推定 (GAE) — 分散低減の決定版

アドバンテージ推定量 $\hat A_t$ の設計を正面から扱う。前作8.2節の $n$ ステップTDの議論を、アドバンテージに焼き直すところから始める。クリティック $V \approx V^\pi$ を使い、TD誤差を

$$
\delta_t := r_{t+1} + \gamma V(s_{t+1}) - V(s_t)
$$

と書く。まず、**$k$ ステップアドバンテージ推定量**を定義する:

$$
\hat A_t^{(k)} := \underbrace{r_{t+1} + \gamma r_{t+2} + \cdots + \gamma^{k-1} r_{t+k} + \gamma^k V(s_{t+k})}_{k\text{ステップ収益(}Q\text{の推定)}} - V(s_t)
$$

$k$ が小さいほど低分散・高バイアス($V$ の誤りに依存)、$k \to \infty$ でMC(不偏・高分散)。ここで美しい恒等式が成り立つ:**$k$ ステップアドバンテージはTD誤差の和で書ける**。

$$
\hat A_t^{(k)} = \sum_{l=0}^{k-1} \gamma^l\, \delta_{t+l}
\tag{4.2}
$$

**確認(望遠鏡和):** 右辺を展開すると
$$
\sum_{l=0}^{k-1} \gamma^l \left[ r_{t+l+1} + \gamma V(s_{t+l+1}) - V(s_{t+l}) \right]
= \sum_{l=0}^{k-1} \gamma^l r_{t+l+1} + \sum_{l=0}^{k-1} \left[ \gamma^{l+1} V(s_{t+l+1}) - \gamma^l V(s_{t+l}) \right]
$$
第2の和は望遠鏡式に潰れて $\gamma^k V(s_{t+k}) - V(s_t)$ のみが残り、(4.2)の左辺に一致する。$\blacksquare$

Schulmanら (2016) の**GAE**は、前作のTD($\lambda$)と全く同じ発想で、全 $k$ の推定量を重み $(1-\lambda)\lambda^{k-1}$ で幾何混合する:

$$
\hat A_t^{\mathrm{GAE}(\gamma, \lambda)} := (1 - \lambda) \sum_{k=1}^{\infty} \lambda^{k-1}\, \hat A_t^{(k)}
$$

これを(4.2)を使って計算する。和の順序を交換すると($l$ を固定して $k \ge l+1$ の寄与を集める):

$$
\hat A_t^{\mathrm{GAE}} = (1-\lambda) \sum_{k=1}^\infty \lambda^{k-1} \sum_{l=0}^{k-1} \gamma^l \delta_{t+l}
= \sum_{l=0}^{\infty} \gamma^l \delta_{t+l}\; (1-\lambda) \sum_{k=l+1}^{\infty} \lambda^{k-1}
= \sum_{l=0}^{\infty} \gamma^l \delta_{t+l} \cdot \lambda^{l}
$$

(最後の等号は幾何級数 $(1-\lambda)\sum_{k \ge l+1} \lambda^{k-1} = (1-\lambda)\frac{\lambda^l}{1-\lambda} = \lambda^l$。)よって:

> [!note] 定理 4.1(GAEの閉形式)
> $$
> \hat A_t^{\mathrm{GAE}(\gamma, \lambda)} = \sum_{l=0}^{\infty} (\gamma \lambda)^l\, \delta_{t+l}
> $$
> すなわちGAEは、**TD誤差を割引率 $\gamma\lambda$ で割り引いて足し合わせたもの**である。$\lambda = 0$ で $\hat A_t = \delta_t$(1ステップTD:低分散・高バイアス)、$\lambda = 1$ で $\hat A_t = \sum_l \gamma^l \delta_{t+l} = G_t - V(s_t)$(MCベースライン付き:不偏・高分散)に帰着し、$\lambda \in (0,1)$ が両者を連続に補間する。

実装は後ろ向きの再帰 $\hat A_t = \delta_t + \gamma\lambda\, \hat A_{t+1}$ で $O(T)$ で済む。実務では $\lambda \approx 0.95$–$0.97$ が広く使われ、GAEはA2C以降のほぼすべての方策オン型手法(特に次章のPPO)の標準部品となっている。

> [!tip] 物理の直観:2つの減衰長
> GAEには2つの「相関長」がある。$\gamma$ は**問題そのもの**が定める報酬の時間スケール($\tau \sim 1/(1-\gamma)$、前作2.1節)であり、$\lambda$ は**推定器**が「クリティックをどこまで信用するか」を決める信用の減衰長である。遠方($l$ 大)のTD誤差は分散源なので $\lambda$ で追加減衰させて切り捨て、その分の情報はクリティックのブートストラップで代用する——2種類の遮蔽長をもつ応答関数の設計、と読める。

---

# 第5章 深層方策勾配 (II) — 性能差分補題・自然勾配・TRPO・PPO

## 5.1 問題の所在:方策勾配はなぜ「大きな一歩」で壊れるのか

A2Cは動くが、学習率に極めて敏感で、しばしば性能が突然崩壊する。原因は方策勾配法の構造にある。勾配(4.1)の期待値は**現在の方策の状態分布 $d^{\pi_{\boldsymbol\theta}}$** でとられている。パラメータを大きく動かすと:

1. 手元のデータ(古い方策由来)で推定した勾配は、新しい方策では**もはや勾配ではない**(分布 $d^\pi$ が動いたため)。
2. さらに悪いことに、方策の悪化 → 訪れる状態の質の悪化 → データの質の悪化 → さらに悪い更新、という**自己増悪ループ**に入りうる。教師あり学習ではデータ分布が固定なので一歩失敗しても次で回復できるが、強化学習ではデータを自分で生成しているため、悪い方策への一歩が回復不能になりうる。

したがって問うべきは:**「古い方策 $\pi$ のデータだけを使って、新しい方策 $\pi'$ の性能を(保証付きで)評価・改善するにはどうすればよいか」**である。この問いに厳密に答えるのが本章であり、その頂点にPPO——現在最も広く使われる深層RLアルゴリズム——がある。

## 5.2 性能差分補題 — 新旧方策の性能差の厳密な表現

すべての出発点となる恒等式を証明する。

> [!note] 定理 5.1(性能差分補題; Kakade & Langford, 2002)
> 任意の2方策 $\pi, \pi'$ に対して、
> $$
> J(\pi') - J(\pi) = \mathbb E_{\tau \sim \pi'} \left[ \sum_{t=0}^{\infty} \gamma^t\, A^{\pi}(s_t, a_t) \right]
> = \frac{1}{1-\gamma}\, \mathbb E_{s \sim \bar d^{\pi'},\, a \sim \pi'} \left[ A^\pi(s, a) \right]
> $$
> ここで $\tau \sim \pi'$ は初期分布 $\rho_0$ から $\pi'$ で生成した軌道、$\bar d^{\pi'}$ は $\pi'$ の正規化された割引訪問分布(前作10.2節の $d^{\pi'}$ を $(1-\gamma)$ 倍したもの)。

**証明。** アドバンテージの定義と $Q^\pi$ のBellman方程式(前作(3.2))より、任意の $(s_t, a_t)$ と、その後 $P$ で生成される $s_{t+1}$ に対して
$$
A^\pi(s_t, a_t) = Q^\pi(s_t, a_t) - V^\pi(s_t) = \mathbb E_{s_{t+1}}\left[ r_{t+1} + \gamma V^\pi(s_{t+1}) - V^\pi(s_t) \right]
$$
(遷移は方策によらず $P$ に従うので、この期待値は $\tau \sim \pi'$ の軌道上でも正しい。)これを右辺に代入すると:
$$
\mathbb E_{\tau \sim \pi'} \left[ \sum_{t=0}^\infty \gamma^t A^\pi(s_t, a_t) \right]
= \mathbb E_{\tau \sim \pi'} \left[ \sum_{t=0}^\infty \gamma^t r_{t+1} + \sum_{t=0}^\infty \left( \gamma^{t+1} V^\pi(s_{t+1}) - \gamma^t V^\pi(s_t) \right) \right]
$$
第1の和は $J(\pi')$ の中身、第2の和は望遠鏡式に潰れて $-V^\pi(s_0)$ が残る($\gamma^t V^\pi(s_t) \to 0$ は $V^\pi$ の有界性による)。$s_0 \sim \rho_0$ で平均して
$$
= J(\pi') - \mathbb E_{s_0 \sim \rho_0}[V^\pi(s_0)] = J(\pi') - J(\pi) \qquad \blacksquare
$$

> [!tip] この補題の読み方
> 「新方策の性能向上分 = **新方策が訪れる状態で、新方策がとる行動の、旧方策から見たアドバンテージ**の合計」。前作6.2節の方策改善定理(すべての状態で $A^\pi(s, \pi'(s)) \ge 0$ なら改善)は、この恒等式の直接の系である(右辺の被積分関数が非負なら左辺も非負)。つまり性能差分補題は**方策改善定理の等式版・定量版**であり、GPIの「改善」プロセスの完全な解剖図である。

## 5.3 代理目的関数と、その信頼できる範囲

補題5.1の右辺を $\pi'$ について最大化したいが、期待値が**未知の分布 $\bar d^{\pi'}$**(新方策の訪問分布)でとられているため、そのままでは使えない。そこで**状態分布だけを旧方策のもの $\bar d^{\pi}$ で近似**する(行動分布のずれは重点サンプリング比で厳密に扱える。前作7.4節):

$$
L_\pi(\pi') := J(\pi) + \frac{1}{1-\gamma}\, \mathbb E_{s \sim \bar d^{\pi},\ a \sim \pi} \left[ \frac{\pi'(a|s)}{\pi(a|s)}\, A^\pi(s, a) \right]
$$

これを**代理目的関数 (surrogate objective)** と呼ぶ。$L_\pi$ は旧方策のデータだけで推定できる。近似の質については次が言える:$\pi' = \pi$ で $L_\pi(\pi) = J(\pi)$(値の一致)、かつパラメータ微分も一致($\nabla_{\boldsymbol\theta'} L_\pi(\pi_{\boldsymbol\theta'})|_{\boldsymbol\theta'=\boldsymbol\theta} = \nabla J$:これは方策勾配定理の言い換えである)。すなわち $L_\pi$ は $J$ の**1次のTaylor近似として正しい**。問題は $\pi'$ が $\pi$ から離れたときで、Schulmanら (2015) は次の下界を証明した:

$$
J(\pi') \ge L_\pi(\pi') - C \cdot \max_s D_{\mathrm{KL}}\!\left( \pi(\cdot|s) \,\|\, \pi'(\cdot|s) \right),
\qquad C = \frac{4 \epsilon \gamma}{(1-\gamma)^2},\quad \epsilon = \max_{s,a} |A^\pi(s,a)|
$$

意味は明快である:**代理目的を上げつつ、方策のKL的な移動距離をペナルティ以内に抑えれば、真の性能の向上が保証される**(この下界を各反復で最大化すれば単調改善列が得られる:MM算法)。「一歩の大きさをパラメータ空間のユークリッド距離ではなく、**方策の分布としての距離**で測って制限せよ」——これが信頼領域 (trust region) の思想である。

## 5.4 自然勾配 — 分布空間の幾何学

「分布としての距離で測る」を勾配法に組み込むと**自然勾配**になる。通常の勾配法は $\|\Delta\boldsymbol\theta\|^2 \le \epsilon$ の制約下で $J$ を最大化する一歩(=ユークリッド計量での最急上昇)だが、パラメータ空間の距離は分布の変化を正しく反映しない(例:ガウス方策の平均を $0.1$ 動かす効果は、分散が小さいほど劇的である)。そこで一歩の大きさをKLダイバージェンスで測る:

$$
\max_{\Delta\boldsymbol\theta}\ \nabla J^\top \Delta\boldsymbol\theta
\quad \text{s.t.} \quad \overline{D}_{\mathrm{KL}}\!\left( \pi_{\boldsymbol\theta} \,\|\, \pi_{\boldsymbol\theta + \Delta\boldsymbol\theta} \right) \le \epsilon
$$

KLを $\Delta\boldsymbol\theta$ の2次まで展開すると(1次の項は消える:付録A)、

$$
\overline{D}_{\mathrm{KL}} \approx \frac12\, \Delta\boldsymbol\theta^\top F(\boldsymbol\theta)\, \Delta\boldsymbol\theta,
\qquad
F(\boldsymbol\theta) = \mathbb E_{s,a}\left[ \nabla_{\boldsymbol\theta} \ln \pi_{\boldsymbol\theta}(a|s)\, \nabla_{\boldsymbol\theta} \ln \pi_{\boldsymbol\theta}(a|s)^\top \right]
$$

$F$ は**Fisher情報行列**である。Lagrange未定乗数法により、最適な一歩は

$$
\Delta\boldsymbol\theta \propto F^{-1}\, \nabla J
$$

これを**自然勾配** (Amari, 1998) と呼ぶ。幾何学的には、$F$ は分布族という多様体上のRiemann計量(情報幾何)であり、自然勾配は**この計量に関する最急上昇方向**、すなわちパラメータ化の取り方に不変な勾配である。物理でいえば、一般座標系での勾配を計量テンソルで補正して共変ベクトルから反変ベクトルへ直す操作($\partial^i = g^{ij}\partial_j$)そのものである。

## 5.5 TRPO

**TRPO** (Trust Region Policy Optimization; Schulman et al., 2015) は、5.3節の理論を実装可能な形に落としたものである。理論のmax-KLペナルティは保守的すぎるので、平均KLの**制約**に置き換える:

$$
\max_{\boldsymbol\theta'}\ \hat{\mathbb E}\left[ \frac{\pi_{\boldsymbol\theta'}(a|s)}{\pi_{\boldsymbol\theta}(a|s)}\, \hat A(s,a) \right]
\quad \text{s.t.} \quad \hat{\mathbb E}\left[ D_{\mathrm{KL}}(\pi_{\boldsymbol\theta} \| \pi_{\boldsymbol\theta'}) \right] \le \delta
$$

目的を1次、制約を2次で近似すると自然勾配の問題に帰着し、$F^{-1}\nabla J$ を共役勾配法で(逆行列を作らずFisher-ベクトル積だけで)解き、直線探索で制約充足と代理目的の改善を確認して一歩進む。TRPOは連続制御・ロコモーションで当時の最高性能を示し、「単調改善の理論に裏打ちされた深層RL」の到達点となった。しかし、共役勾配と直線探索は実装が重く、ミニバッチ・共有パラメータ・再帰型ネットとの相性も悪い。**同じ思想を1次の勾配法だけで実現できないか?**——それがPPOである。

## 5.6 PPO — クリッピングによる信頼領域の1次近似

**PPO** (Proximal Policy Optimization; Schulman et al., 2017) は、確率比 $\rho_t(\boldsymbol\theta') := \dfrac{\pi_{\boldsymbol\theta'}(a_t|s_t)}{\pi_{\boldsymbol\theta}(a_t|s_t)}$ を使い、次の**クリップ付き代理目的**を最大化する:

$$
L^{\mathrm{CLIP}}(\boldsymbol\theta') = \hat{\mathbb E}_t \left[ \min\!\left( \rho_t \hat A_t,\ \mathrm{clip}(\rho_t,\, 1-\epsilon,\, 1+\epsilon)\, \hat A_t \right) \right]
$$

($\epsilon \approx 0.1$–$0.2$。)この式の挙動を場合分けで精読しよう。ポイントは $\min$ と符号の相互作用である:

- **$\hat A_t > 0$(良い行動)の場合:** その行動の確率を上げたい($\rho_t$ を増やしたい)。$\rho_t$ が $1+\epsilon$ を超えるとクリップ項が選ばれ、目的関数は $\rho_t$ に関して**平坦になる=勾配が消える**。つまり「良い行動でも、確率を一度に $1+\epsilon$ 倍より増やす方向のインセンティブは与えない」。
- **$\hat A_t < 0$(悪い行動)の場合:** 確率を下げたい。$\rho_t$ が $1-\epsilon$ を下回ると同様に勾配が消える。「悪い行動でも一度に潰しすぎない」。
- **重要な非対称性:** $\min$ をとっているため、クリップは**目的関数を改善する方向の更新だけを頭打ちにする**。逆方向(例:$\hat A_t>0$ なのに $\rho_t < 1-\epsilon$ になってしまった場合)にはクリップは働かず、元に戻す勾配は生き続ける。つまり $L^{\mathrm{CLIP}}$ は素の代理目的の**下界**であり、悲観的(保守的)な近似になっている。

こうしてPPOは、TRPOのKL制約を「更新のインセンティブ設計」で置き換え、**通常のSGD(Adam)で数エポック、同じデータを回して**大きめの更新を安全に行えるようにした。実装全体は:並列環境でロールアウト収集 → GAE(第4章)でアドバンテージ計算 → $L^{\mathrm{CLIP}} - c_1 L^{\mathrm{VF}} + c_2 \mathcal H$(クリティック損失とエントロピーボーナスを合成)をミニバッチで数エポック最適化、の繰り返しである。単純さ・頑健さ・並列性のバランスから、PPOはロボット制御からゲーム、そして大規模言語モデルのRLHF(第9章)まで、**現在の事実上の標準アルゴリズム**となっている。

> [!important] 第5章のまとめ:理論の系譜
> 方策改善定理(前作6.2節)→ 性能差分補題(等式化)→ 代理目的+KL下界(旧データでの安全な改善)→ TRPO(制約付き最適化+自然勾配)→ PPO(1次法への蒸留)。PPOのクリッピングは天下り的な小技に見えるが、実際には**前作の方策改善定理から一本道でつながる理論の末端**である。

---

# 第6章 連続行動制御 — 決定論的方策勾配・DDPG・TD3

## 6.1 問題設定:連続行動空間でのQ学習の困難

ロボットの関節トルクのような連続行動空間 $\mathcal A \subseteq \mathbb R^m$ では、Q学習系の手法は目標値の $\max_{a'} Q(s', a')$ が**それ自体が連続最適化問題**になり、毎ステップ解くのは高コストである。第4–5章の確率的方策勾配は連続行動を扱えるが、方策オン型ゆえサンプル効率が低い。ここで問う:**方策オフ型(経験再生が使える)で、連続行動を直接扱える方法はないか。**

答えの鍵は、方策を**決定論的関数** $a = \mu_{\boldsymbol\theta}(s)$ にすることである。すると $\max_{a'} Q(s', a')$ の代わりに $Q(s', \mu_{\boldsymbol\theta}(s'))$ を使えばよく(「$\arg\max$ をネットワーク $\mu$ に覚えさせる」)、さらに $Q$ が $a$ について微分可能なら、**$Q$ を $a$ 方向に登る勾配を、連鎖律で方策パラメータまで流せる**。

## 6.2 決定論的方策勾配 (DPG) 定理の導出

目的関数は $J(\boldsymbol\theta) = \mathbb E_{s_0 \sim \rho_0}[V^{\mu_{\boldsymbol\theta}}(s_0)]$。前作10.2節の方策勾配定理の導出をなぞる。決定論的方策では $V^\mu(s) = Q^\mu(s, \mu(s))$ なので、

$$
\nabla_{\boldsymbol\theta} V^\mu(s) = \nabla_{\boldsymbol\theta} Q^\mu(s, \mu_{\boldsymbol\theta}(s))
= \underbrace{\nabla_{\boldsymbol\theta}\mu_{\boldsymbol\theta}(s)\, \nabla_a Q^\mu(s, a)\big|_{a=\mu(s)}}_{\text{方策の直接効果}}
+ \underbrace{\nabla_{\boldsymbol\theta} Q^\mu(s, a)\big|_{a=\mu(s)}}_{Q^\mu \text{自身の } \boldsymbol\theta \text{依存}}
$$

第2項に $Q^\mu(s,a) = R(s,a) + \gamma \int P(s'|s,a) V^\mu(s')\, ds'$ を代入すると($R, P$ は $\boldsymbol\theta$ 非依存)$\gamma \int P(s'|s,\mu(s))\, \nabla_{\boldsymbol\theta} V^\mu(s')\, ds'$ となり、前作(10.1)と同じ「ソース項+割引伝播」の再帰式が得られる。同様に展開(unroll)して:

> [!note] 定理 6.1(決定論的方策勾配定理; Silver et al., 2014)
> $$
> \nabla_{\boldsymbol\theta} J(\boldsymbol\theta) = \mathbb E_{s \sim d^{\mu}} \left[ \nabla_{\boldsymbol\theta}\, \mu_{\boldsymbol\theta}(s)\; \nabla_a Q^{\mu}(s, a)\big|_{a = \mu_{\boldsymbol\theta}(s)} \right]
> $$

確率的方策勾配(スコア関数 $\nabla \ln \pi$ を使う)と比べ、**行動空間上の期待値が消えている**(方策が1点分布だから)。これは分散の観点で大きな利点である:確率的勾配は行動のサンプリングノイズを含むが、DPGは $Q$ の $a$ 微分という決定論的な情報を直接使う。実際、DPGは方策の分散を0に近づけた確率的方策勾配の極限として導出できることも示されている。代償は**探索が方策自身からは生まれない**ことで、行動に外生ノイズを加える(例:$a = \mu(s) + \mathcal N(0, \sigma^2)$)必要がある。挙動方策(ノイズ付き)と目標方策(決定論的 $\mu$)が異なるので、本質的に**方策オフ型**である——だから経験再生が使える。

## 6.3 DDPG — DPG × DQN

**DDPG** (Lillicrap et al., 2016) は、DPG定理にDQNの安定化装置一式を移植したものである:

- **クリティック** $Q_{\boldsymbol w}(s, a)$(行動を入力に取る点がDQNと異なる)を、リプレイバッファからのミニバッチで、目標
$$
y = r + \gamma\, Q_{\boldsymbol w^-}\!\left( s',\ \mu_{\boldsymbol\theta^-}(s') \right)
$$
への回帰で学習(ターゲットネットは方策・クリティックの両方に用意し、Polyak平均 $\boldsymbol w^- \leftarrow \tau \boldsymbol w + (1-\tau)\boldsymbol w^-$ で緩やかに追従)。
- **アクター** $\mu_{\boldsymbol\theta}$ は、定理6.1のサンプル版
$$
\nabla_{\boldsymbol\theta} J \approx \frac1B \sum_i \nabla_{\boldsymbol\theta} \mu_{\boldsymbol\theta}(s_i)\, \nabla_a Q_{\boldsymbol w}(s_i, a)\big|_{a = \mu_{\boldsymbol\theta}(s_i)}
$$
で更新。実装上は「損失 $-\frac1B\sum_i Q_{\boldsymbol w}(s_i, \mu_{\boldsymbol\theta}(s_i))$ を $\boldsymbol\theta$ について降下」と書けば自動微分が上式を計算してくれる。

DDPGは連続制御ベンチマークで高いサンプル効率を示したが、**極めて脆い**ことでも知られる。主因はクリティックの過大評価である:アクターは $Q_{\boldsymbol w}$ を**能動的に登る**ため、$Q_{\boldsymbol w}$ が誤って高い値をつけた領域(近似誤差の山)を探し出して搾取する。$\max_{a'}$ による過大評価(3.1節)よりさらに性質が悪い——**最大化が勾配上昇によって明示的・執拗に行われる**からである。

## 6.4 TD3 — 過大評価との三段構えの戦い

**TD3** (Twin Delayed DDPG; Fujimoto et al., 2018) は、この病理を3つの機構で抑え込む:

1. **Clipped Double Q学習:** 2つの独立なクリティック $Q_{\boldsymbol w_1}, Q_{\boldsymbol w_2}$ を学習し、目標値に**小さい方**を使う:
$$
y = r + \gamma\, \min_{j=1,2} Q_{\boldsymbol w_j^-}\!\left( s',\ \tilde a' \right)
$$
Double DQN(3.1節)の「選択と評価の分離」よりさらに強い、**明示的な悲観化**である。過小評価のバイアスを導入することになるが、過小評価は(アクターがそこを避けるだけで)伝播・増幅しにくいのに対し、過大評価はアクターに搾取され増幅するため、**バイアスの符号には非対称な危険性がある**、という洞察に基づく。
2. **ターゲット方策平滑化:** 目標行動にクリップしたノイズを加える:$\tilde a' = \mu_{\boldsymbol\theta^-}(s') + \mathrm{clip}(\mathcal N(0, \sigma^2), -c, c)$。Q関数の鋭いピーク(近似誤差)を目標値の段階で平滑化し、「価値は行動の小さな摂動に対して滑らかであるべき」という正則化(一種のSARSA的期待化)をかける。
3. **遅延方策更新:** アクターの更新頻度をクリティックの半分にする(クリティック2回にアクター1回)。価値推定が落ち着く前に方策がそれを搾取するのを防ぐ。前作GPIの言葉でいえば、「評価」をある程度進めてから「改善」する、という粒度の調整である。

TD3はDDPGを大差で上回り、決定論的方策系の標準となった。ここで採用された $\min$ による悲観化は、次章のSACにもそのまま輸出される。


---

# 第7章 最大エントロピー強化学習 — ソフトBellman方程式とSAC

本章は、物理学の読者にとって本テキストで最も「ホーム」に感じられる章だろう。強化学習の目的関数にエントロピーを組み込むと、理論全体が**統計力学のカノニカル形式**と構造的に同型になる。

## 7.1 目的関数の変更:報酬とエントロピーの同時最大化

4.2節のエントロピーボーナスは、探索を保つための正則化として天下りに導入した。最大エントロピーRLは、これを**目的関数の原理的な変更**として正面から採用する:

$$
J_{\mathrm{MaxEnt}}(\pi) = \mathbb E_{\tau \sim \pi} \left[ \sum_{t=0}^\infty \gamma^t \left( r_{t+1} + \alpha\, \mathcal H\big(\pi(\cdot \mid s_t)\big) \right) \right]
$$

ここで $\alpha > 0$ は**温度パラメータ**である。エージェントは「報酬を稼ぎつつ、**可能な限りランダムに**振る舞う」ことを求められる。動機は3つある:

1. **探索の原理化:** 高報酬の行動が複数あれば、そのすべてに確率を割り振る方策が最適になる。探索がアドホックなノイズではなく最適解の性質そのものになる。
2. **頑健性:** 複数の解を保持する方策は、環境の摂動や下流タスクへの転移に強い。
3. **最適化地形の平滑化:** エントロピー項は方策空間の目的関数を強凹化(各状態で)し、学習を安定させる。

$\alpha \to 0$ で通常のRLに戻る。以下で見る通り、$\alpha$ は文字通り統計力学の温度の役割を果たし、$\alpha \to 0$ は「基底状態(貪欲方策)への冷却」に対応する。

## 7.2 ソフトBellman方程式の導出

前作第3–5章のプログラムを、この新しい目的関数のもとで再実行する。ソフト価値関数を

$$
V_{\mathrm{soft}}^\pi(s) := \mathbb E_\pi\left[ \sum_{t=0}^\infty \gamma^t \left( r_{t+1} + \alpha \mathcal H(\pi(\cdot|s_t)) \right) \,\middle|\, s_0 = s \right]
$$

$$
Q_{\mathrm{soft}}^\pi(s, a) := \mathbb E\left[ r_{1} + \gamma V_{\mathrm{soft}}^\pi(s_1) \,\middle|\, s_0 = s, a_0 = a \right]
$$

と定義する($Q$ の定義では時刻0のエントロピーは含めない流儀をとる)。前作(2.2)に対応する関係は、エントロピーの定義 $\mathcal H(\pi) = -\mathbb E_{a \sim \pi}[\ln \pi(a|s)]$ を書き下せば

$$
V_{\mathrm{soft}}^\pi(s) = \mathbb E_{a \sim \pi}\left[ Q_{\mathrm{soft}}^\pi(s, a) - \alpha \ln \pi(a|s) \right]
\tag{7.1}
$$

となる(導出:$V^\pi_{\mathrm{soft}}(s)$ の第0項 $r_1 + \alpha\mathcal H$ を分離し、$\alpha\mathcal H(\pi(\cdot|s)) = -\alpha \mathbb E_a[\ln\pi(a|s)]$ を $a$ の期待値の中に入れ、残りを $Q_{\mathrm{soft}}$ にまとめる)。(7.1)と $Q$ の定義を組み合わせたものが**ソフトBellman期待方程式**である。

**最適方策の構造。** 次に、状態 $s$ における(7.1)右辺を、$Q_{\mathrm{soft}}$ を固定して分布 $\pi(\cdot|s)$ について最大化する。これは制約 $\sum_a \pi(a|s) = 1$ 付きの変分問題:

$$
\max_{\pi(\cdot|s)}\ \sum_a \pi(a|s) \left[ Q(s,a) - \alpha \ln \pi(a|s) \right]
$$

Lagrange関数 $\mathcal F = \sum_a \pi_a (Q_a - \alpha \ln \pi_a) + \lambda (\sum_a \pi_a - 1)$ を $\pi_a$ で微分してゼロと置くと、

$$
Q_a - \alpha \ln \pi_a - \alpha + \lambda = 0
\quad\Longrightarrow\quad
\pi_a \propto \exp\!\left( \frac{Q_a}{\alpha} \right)
$$

規格化して:

> [!note] 定理 7.1(最大エントロピー最適方策はBoltzmann分布)
> ソフトQ関数 $Q_{\mathrm{soft}}$ に対する各状態での最適方策は
> $$
> \pi^*(a \mid s) = \frac{\exp\left( Q_{\mathrm{soft}}(s,a)/\alpha \right)}{Z(s)},
> \qquad Z(s) = \sum_{a} \exp\left( Q_{\mathrm{soft}}(s,a)/\alpha \right)
> $$
> であり、このときの最大値(=ソフト状態価値)は
> $$
> V_{\mathrm{soft}}(s) = \alpha \ln \sum_a \exp\left( \frac{Q_{\mathrm{soft}}(s, a)}{\alpha} \right) = \alpha \ln Z(s)
> $$

(最大値の確認:$\pi^*$ を(7.1)右辺に代入すると $\ln \pi^* = Q/\alpha - \ln Z$ より $\mathbb E_{\pi^*}[Q - \alpha(Q/\alpha - \ln Z)] = \alpha \ln Z$。)これを $Q$ の定義に代入すると、**ソフトBellman最適方程式**を得る:

$$
Q_{\mathrm{soft}}^*(s, a) = R(s, a) + \gamma\, \mathbb E_{s'}\left[ \alpha \ln \sum_{a'} \exp\!\left( \frac{Q_{\mathrm{soft}}^*(s', a')}{\alpha} \right) \right]
$$

前作のBellman最適方程式と比べると、**$\max_{a'}$ が log-sum-exp(ソフトマックス)に置き換わっている**。log-sum-expは $\max$ の滑らかな近似であり、$\alpha \to 0$ の極限で

$$
\alpha \ln \sum_a e^{Q_a/\alpha} \xrightarrow{\alpha \to 0} \max_a Q_a
$$

となって通常のBellman最適方程式に戻る(「ソフト」の名の由来)。ソフトBellman作用素もsupノルムで $\gamma$-縮小であることが(log-sum-expの非拡大性 $|\alpha\ln\sum e^{x_a/\alpha} - \alpha\ln\sum e^{y_a/\alpha}| \le \max_a|x_a - y_a|$ を前作の補題5.3の代わりに使って)全く同様に証明でき、**前作第5章の理論——不動点の存在・一意性・反復収束——が丸ごと再利用できる**。

> [!example] 統計力学との対応(本章の白眉)
> 対応表を作ると:
>
> | 強化学習 | 統計力学 |
> | ---- | ---- |
> | ソフトQ関数 $Q_{\mathrm{soft}}(s,a)$ | エネルギーの符号反転 $-E_a$ |
> | 温度パラメータ $\alpha$ | 温度 $k_B T$ |
> | 最適方策 $\pi^* \propto e^{Q/\alpha}$ | カノニカル分布 $p_a \propto e^{-E_a / k_B T}$ |
> | ソフト価値 $V = \alpha \ln Z$ | 自由エネルギーの符号反転 $-F = k_B T \ln Z$ |
> | (7.1)式 $V = \mathbb E_\pi[Q] + \alpha\mathcal H$ | $-F = -\langle E \rangle + TS$ すなわち $F = \langle E\rangle - TS$ |
> | $\alpha \to 0$ で貪欲方策へ | 零温度極限で基底状態へ |
>
> 最大エントロピーRLとは、**各状態の行動選択を「$-Q$ をエネルギーとする有限温度のカノニカル系」として扱い、自由エネルギー(の符号反転)をBellman再帰で自己無撞着に決める理論**である。エネルギー最小化(報酬最大化)とエントロピー最大化の競合という、物理学者が骨の髄まで知っている変分原理が、意思決定の文脈で寸分違わず再現されている。

## 7.3 ソフト方策反復

理論の最後の部品として、GPI(前作6.5節)のソフト版を確認する。

- **ソフト方策評価:** ソフトBellman期待作用素の反復で $Q_{\mathrm{soft}}^\pi$ に収束(縮小性より)。
- **ソフト方策改善:** 現在の $Q_{\mathrm{soft}}^\pi$ に対するBoltzmann方策
$$
\pi_{\mathrm{new}}(\cdot|s) = \arg\min_{\pi'} D_{\mathrm{KL}}\!\left( \pi'(\cdot|s) \,\middle\|\, \frac{\exp(Q^{\pi_{\mathrm{old}}}_{\mathrm{soft}}(s,\cdot)/\alpha)}{Z(s)} \right)
$$
に更新すると、$Q^{\pi_{\mathrm{new}}}_{\mathrm{soft}} \ge Q^{\pi_{\mathrm{old}}}_{\mathrm{soft}}$(すべての $(s,a)$ で)が成り立つ——**ソフト方策改善定理**。証明は前作の定理6.1の証明(1ステップずつ新方策に置き換える望遠鏡展開)のソフト版であり、KL最小として書いたのは、後述のSACで方策を制限された関数族(ガウス方策)に射影する場合にも改善が保証される形にするためである。

この交互反復(**ソフト方策反復**)は表形式なら最大エントロピー最適方策に収束する。SACはこれの関数近似版である。

## 7.4 SAC (Soft Actor-Critic)

**SAC** (Haarnoja et al., 2018) は、ソフト方策反復を、連続行動・DNN・方策オフ(経験再生)の設定で近似実行する。構成要素は:

**(1) ソフトクリティック学習。** TD3から輸入したclipped double Q($\min_{j=1,2}$)とターゲットネットを使い、リプレイバッファのミニバッチで、目標

$$
y = r + \gamma \left[ \min_{j} Q_{\boldsymbol w_j^-}(s', a') - \alpha \ln \pi_{\boldsymbol\theta}(a' \mid s') \right],
\qquad a' \sim \pi_{\boldsymbol\theta}(\cdot \mid s')
$$

への二乗回帰を行う。角括弧内は(7.1)のサンプル版(次状態のソフト価値の1サンプル推定)である。次行動 $a'$ を**現在の方策からサンプルし直す**(バッファ内の実際の次行動を使わない)点が、方策オフでありながらソフトBellman**期待**方程式を現在の方策について解いている、という位置づけを与える。

**(2) アクター学習と再パラメータ化勾配。** 方策改善は、7.3節のKL最小化を勾配法で行う。定数(log Z)を除けば損失は

$$
\mathcal L_\pi(\boldsymbol\theta) = \mathbb E_{s \sim \mathcal D}\, \mathbb E_{a \sim \pi_{\boldsymbol\theta}(\cdot|s)} \left[ \alpha \ln \pi_{\boldsymbol\theta}(a|s) - \min_j Q_{\boldsymbol w_j}(s, a) \right]
$$

内側の期待値の分布が最適化変数 $\boldsymbol\theta$ 自身に依存するため、勾配計算に工夫が要る。REINFORCE型のスコア関数勾配(前作10.2節)も使えるが高分散なので、SACは**再パラメータ化 (reparameterization trick)** を使う:ガウス方策 $a \sim \mathcal N(\mu_{\boldsymbol\theta}(s), \sigma_{\boldsymbol\theta}(s)^2)$ のサンプルを、パラメータ非依存のノイズ $\epsilon \sim \mathcal N(0, I)$ の決定論的変換

$$
a = f_{\boldsymbol\theta}(s, \epsilon) = \mu_{\boldsymbol\theta}(s) + \sigma_{\boldsymbol\theta}(s) \odot \epsilon
$$

として書き直す(実際は行動を有界化するため $\tanh$ を通し、変数変換のヤコビアンで対数密度を補正する)。すると期待値の分布が $\boldsymbol\theta$ 非依存の $\epsilon$ に移り、勾配を期待値の中に入れて

$$
\nabla_{\boldsymbol\theta} \mathcal L_\pi = \mathbb E_{s, \epsilon} \left[ \nabla_{\boldsymbol\theta} \left( \alpha \ln \pi_{\boldsymbol\theta}(f_{\boldsymbol\theta}(s,\epsilon) \mid s) - \min_j Q_{\boldsymbol w_j}(s, f_{\boldsymbol\theta}(s, \epsilon)) \right) \right]
$$

と、**$Q$ の $a$ 微分を連鎖律で方策まで流す**低分散推定量が得られる(DPG(6.2節)の「$Q$ を登る」構造がここに再登場している。スコア関数勾配との系統的比較は付録B)。

**(3) 温度の自動調整。** $\alpha$ の適値はタスクの報酬スケールに依存し、手調整が煩わしい。SACの改良版は、目的を「エントロピーが目標値 $\bar{\mathcal H}$ 以上」という制約付き問題に置き換え、その双対変数として $\alpha$ を勾配法で自動調整する:

$$
\mathcal L(\alpha) = \mathbb E_{s, a \sim \pi}\left[ -\alpha \left( \ln \pi(a|s) + \bar{\mathcal H} \right) \right]
$$

(方策のエントロピーが目標より下がると $\alpha$ が増えて探索を強制し、上回ると $\alpha$ が減って報酬追求に集中する、という負帰還。)

SACは連続制御において、サンプル効率(方策オフ+経験再生)と安定性・頑健性(エントロピー正則化+二重クリティック)を高水準で両立し、TD3と並ぶ、実ロボット学習を含む標準手法となっている。

> [!important] 両柱の合流
> SACには本テキストの主要な系譜がすべて流れ込んでいる:Bellman方程式と縮小性(前作)、経験再生とターゲットネット(第2章)、二重推定による悲観化(第3・6章)、actor-criticと再パラメータ化(第4章・付録B)、そして最大エントロピー原理(本章)。**深層強化学習の一つの到達点**として、その設計のすべての行に理論的な由来がある。

---

# 第8章 モデルベースRLとプランニング — MCTS・AlphaZero・MuZero・世界モデル

## 8.1 第三の柱:モデルを学び、モデルの中で考える

ここまでの手法はすべて**モデルフリー**——環境の遷移則 $P$ を推定せず、価値・方策だけを学ぶ——だった。前作第6章で見た通り、モデル $(P, R)$ があれば動的計画法・プランニングという強力な武器が使える。**モデルベースRL**は、(a) 環境モデルをデータから学習し、(b) そのモデルを使ってプランニング(先読み)または仮想経験の生成を行う枠組みである。利点は**サンプル効率**(実環境との相互作用は高価だが、学習済みモデル内のシミュレーションはほぼ無料)、弱点は**モデル誤差の複利的増幅**(不正確なモデルで長い先読みをすると、誤差が各ステップで累積し、プランナーはモデルの誤りを搾取する——6.3節でアクターがクリティックの誤りを搾取したのと同型の病理)である。

古典的な統合枠組みが**Dyna** (Sutton, 1990):実経験でQ学習を行いつつ、同じ経験でモデルを学習し、モデルから生成した仮想遷移でも追加のQ学習更新を行う。「実データで錨を下ろし、モデルで水増しする」という思想は、現代の手法(MBPOなど:モデル展開を短く保って誤差累積を抑えつつSACのバッファを増強する)にも受け継がれている。

## 8.2 モンテカルロ木探索 (MCTS)

モデル(あるいはシミュレータ)があるとき、現在の状態からの**意思決定時プランニング**の代表がMCTSである。現在状態を根とする木を、次の4段階の反復で非対称に成長させる:

1. **選択:** 根から、各ノードで統計量に基づき有望な枝を選んで葉まで降りる。
2. **展開:** 葉に子ノードを追加する。
3. **評価:** 葉の価値を推定する(古典的にはランダムプレイアウト、現代はニューラル価値関数)。
4. **バックアップ:** 評価値を根までの経路上のノードに伝播し、訪問回数 $N(s,a)$ と平均価値 $\hat Q(s,a)$ を更新する。

選択段階の核心は**探索と活用のバランス**であり、UCB(信頼上界)型の指標を使う。AlphaGo系で使われる**PUCT**則は:

$$
a_{\mathrm{select}} = \arg\max_a \left[ \hat Q(s, a) + c\, \pi_{\boldsymbol\theta}(a|s)\, \frac{\sqrt{\sum_b N(s, b)}}{1 + N(s, a)} \right]
$$

第1項が活用(経験的価値)、第2項が探索ボーナス(訪問が少ないほど大きく、事前方策 $\pi_{\boldsymbol\theta}$ が有望とみなす手ほど大きい)。訪問回数とともに探索項は $1/N$ で減衰し、指標は $\hat Q$ に収束する——不確実性に応じたボーナスという、第9章で再訪する探索の一般原理の実例である。

## 8.3 AlphaZero — 探索を「方策改善作用素」として使う

**AlphaZero** (Silver et al., 2017–18) は、囲碁・チェス・将棋を自己対戦のみで制覇した手法だが、その構造は前作のGPIの言葉で驚くほど簡潔に記述できる。単一のネットワークが方策と価値 $(\pi_{\boldsymbol\theta}(\cdot|s), v_{\boldsymbol\theta}(s))$ を出力し:

1. **改善(=MCTS):** 各局面で、$\pi_{\boldsymbol\theta}$ と $v_{\boldsymbol\theta}$ に誘導されたMCTSを実行する。探索後の根の訪問回数分布 $\pi_{\mathrm{MCTS}}(a|s) \propto N(s,a)^{1/\tau}$ は、**生の $\pi_{\boldsymbol\theta}$ より強い方策**になっている(先読みは方策を改善する:これは前作の「1手先読みして貪欲化すれば改善する」という方策改善定理の多手先読み版である)。この改善された方策で自己対戦を行う。
2. **評価・蒸留(=学習):** 対戦データ $(s, \pi_{\mathrm{MCTS}}, z)$($z$ は最終勝敗)に対し、損失
$$
\mathcal L = \left( z - v_{\boldsymbol\theta}(s) \right)^2 - \pi_{\mathrm{MCTS}}^\top \ln \pi_{\boldsymbol\theta}(\cdot|s) + \text{(正則化)}
$$
を最小化。すなわち**探索が作った改善方策を、ネットワークに教師あり的に蒸留し**、価値は実際の結果に回帰させる。

強くなったネットワークは次の反復でより良い探索を導き、より良い探索はより強い教師信号を作る——**MCTSを「方策改善作用素」、ネットワーク学習を「方策評価+射影」とするGPIの、巨大スケールでの実装**である。前作6.5節で「新しいアルゴリズムはGPIのどの部品かを問え」と述べたが、AlphaZeroはその問いが最も鮮やかに答えられる例である。

## 8.4 MuZero と世界モデル — 「何をモデル化するか」の転回

AlphaZeroは完全なシミュレータ(ゲームのルール)を前提とした。**MuZero** (Schrittwieser et al., 2020) はルールすら知らずに同等以上の性能を達成する。鍵は、**観測空間ではなく潜在空間でモデルを学ぶ**こと:観測を潜在状態に符号化する表現関数、潜在状態と行動から次の潜在状態と報酬を予測するダイナミクス関数、潜在状態から方策と価値を出す予測関数、の3つを学習し、MCTSは潜在空間内で行う。しかも潜在ダイナミクスは「未来の観測を再構成できること」ではなく、**「未来の報酬・価値・方策を正しく予測できること」だけ**を要求されて訓練される(価値等価モデル)。プランニングに必要な情報だけを保持する最小のモデルを学ぶ、という思想である。

関連して、**世界モデル**系の研究(Ha & Schmidhuber 2018; Dreamer系列)は、変分オートエンコーダや再帰状態空間モデルで観測の生成モデルを学び、**学習したモデルが生成する「夢」の中で**actor-criticを訓練する。潜在空間のロールアウトは微分可能なので、価値の勾配をダイナミクスを通じて方策まで直接逆伝播できる点が特徴である。モデルベースRLは、サンプル効率が支配的な制約となる実世界応用(ロボティクス等)で中心的な役割を担いつつある。

---

# 第9章 現代的フロンティア — オフラインRL・探索・RLHF

本章では、これまでの枠組みの前提そのものを問い直す3つの発展を概観する。いずれも本テキストの理論が直接の土台になる。

## 9.1 オフラインRL — 環境と一切相互作用できないとき

医療・自動運転など、試行錯誤が危険または高価な領域では、**固定されたデータセット $\mathcal D$(過去の運用ログ)だけから方策を学びたい**。これが**オフラインRL**である。「方策オフ型のQ学習は任意のデータで動くはずでは?」と思うかもしれないが、決定的な違いがある:オンラインの方策オフ学習では、学習した方策が実際に行動して**自らの誤りを訂正するデータを取ってくる**フィードバックループが存在した。オフラインではそれが断たれる。

具体的な病理は**分布外行動への外挿誤差**である。Q学習の目標 $\max_{a'} Q(s', a')$ の最大化は、**データに一度も現れない行動**の上も走る。そのような行動のQ値は関数近似器の根拠なき外挿であり、正の誤差をもつ行動が $\max$ に選ばれてブートストラップで増幅される(3.1節・6.3節の過大評価の、訂正機会を失った最悪版)。処方箋は大別して:

- **方策制約:** 学習方策を挙動方策(データ生成方策)の近傍に制約する($D_{\mathrm{KL}}$ 制約など。第5章の信頼領域の発想の再登場だが、今回は「古い方策」ではなく「データの支持集合」からの逸脱を制限する)。
- **悲観的価値推定:** 代表例 **CQL** (Conservative Q-Learning) は、通常のBellman誤差に加えて「データ外の行動のQ値を押し下げ、データ内の行動のQ値を押し上げる」正則化項を課し、学習されるQ関数が真の価値の下界になるよう設計する。「不確かなものは低く見積もる」という悲観主義の原理は、TD3の $\min$(6.4節)と同根である。

## 9.2 探索の理論 — $\varepsilon$-貪欲を超えて

$\varepsilon$-貪欲(前作7.3節)は無向的な探索であり、報酬が極端に疎な環境(例:Montezuma's Revenge)ではほぼ無力である(偶然の報酬到達を待つしかない)。より原理的な指針は**「不確実性に向かって探索せよ」**であり、2つの実装系統がある:

- **不確実性ボーナス(Optimism in the face of uncertainty):** 訪問回数の少なさに応じたボーナス報酬 $\propto 1/\sqrt{N(s)}$ を与える。表形式ではUCB理論(バンディット問題における後悔上界の理論)に裏打ちされる。深層版では訪問回数を密度モデルで代用する擬似カウント、予測誤差を新奇性の代理とする**RND**(ランダムに固定したネットワークの出力を予測させ、予測誤差が大きい=見たことがない状態、とする)などがある。8.2節のMCTSの探索項も同じ原理だった。
- **内発的動機づけ:** 「予測できないもの・学習が進むもの」自体を報酬にする(好奇心)。環境ダイナミクスの予測誤差を内的報酬とする手法が代表的だが、確率的なテレビ画面のような「予測不能だが学習不能」なノイズに囚われる問題(noisy TV problem)への対処が要る。

探索は、少数の理論的に整った答え(有限MDPのUCB型アルゴリズムの後悔解析)と、深層設定での多数のヒューリスティクスが共存する、現在も活発なフロンティアである。

## 9.3 RLHF — 大規模言語モデルと強化学習

本テキストの技術が近年最も広い社会的影響を持った応用が、**人間のフィードバックからの強化学習 (RLHF)** による大規模言語モデル (LLM) の調整である。言語生成は自然にMDPとして読める:状態 = プロンプト+生成済みトークン列、行動 = 次トークンの選択、方策 = 言語モデルそのもの、エピソード = 1応答の生成。問題は**報酬関数が存在しない**ことで、RLHFは次の3段階でこれを解決する:

1. **教師あり微調整:** 良い応答の例で言語モデルを微調整し、初期方策 $\pi_{\mathrm{ref}}$ を得る。
2. **報酬モデルの学習:** 同一プロンプトへの2応答の人間による優劣比較データから、報酬モデル $r_\phi$ を学習する。優劣確率をBradley–Terryモデル
$$
\Pr(y_1 \succ y_2) = \sigma\!\left( r_\phi(x, y_1) - r_\phi(x, y_2) \right)
$$
($\sigma$ はシグモイド)で表し、交差エントロピーで学習する。**報酬の差だけが効く**構造(物理でいうエネルギー原点の不定性)に注意。
3. **強化学習:** 学習した報酬を最大化するよう、方策(=LLM)を**PPO**(第5章)で微調整する。ただし報酬モデルは真の人間の選好の不完全な代理なので、素朴に最大化すると報酬モデルの穴を突く縮退した出力に至る(**報酬ハッキング**——クリティックを搾取するアクター(6.3節)、モデルを搾取するプランナー(8.1節)と同じ、本テキスト3度目の登場となる「近似された評価器の搾取」問題)。対策として、参照方策からのKL罰則を報酬に組み込む:
$$
r_{\mathrm{total}} = r_\phi(x, y) - \beta\, D_{\mathrm{KL}}\!\left( \pi_{\boldsymbol\theta}(\cdot|x) \,\|\, \pi_{\mathrm{ref}}(\cdot|x) \right)
$$

この「報酬最大化+参照分布へのKL正則化」という目的は、第7章の最大エントロピーRLの一般化(一様分布の代わりに $\pi_{\mathrm{ref}}$ を基準測度にとったもの)であり、その最適解はやはりBoltzmann型 $\pi^*(y|x) \propto \pi_{\mathrm{ref}}(y|x) \exp(r_\phi(x,y)/\beta)$ になる。この閉形式解を逆用して、報酬モデルとRLループを消去し選好データから直接方策を学習する**DPO** (Direct Preference Optimization) も広く使われている。本テキストの理論(方策勾配、PPO、最大エントロピー原理)が、現代AIの中核部品として稼働している好例である。


---

# 第10章 総括 — 全体を貫く設計原理

## 10.1 系統樹の再掲と、各手法の一行要約

```
Bellman方程式 + 縮小写像 (前作)
 │
 ├─ 価値ベース
 │   DQN      : Q学習 + 経験再生(i.i.d.化) + ターゲットネット(目標凍結)
 │   Double   : 選択と評価の分離で max のバイアスを切除
 │   Dueling  : Q = V + A の分解をアーキテクチャに埋め込む
 │   PER      : TD誤差で優先サンプル + IS重みで補正
 │   分布型   : 期待値でなく収益分布を学ぶ(縮小性はWasserstein距離で存続)
 │   Rainbow  : 上記の統合
 │
 ├─ 方策ベース(確率的)
 │   A2C/A3C  : 並列環境で脱相関 + エントロピー正則化
 │   GAE      : TD(λ)のアドバンテージ版 Σ(γλ)^l δ
 │   TRPO     : 性能差分補題 → 代理目的 + KL信頼領域 + 自然勾配
 │   PPO      : 信頼領域を確率比クリッピングで1次法に蒸留
 │
 ├─ 方策ベース(決定論的・連続行動)
 │   DPG/DDPG : ∇a Q を連鎖律で方策へ + DQNの安定化装置
 │   TD3      : min二重クリティック + 目標平滑化 + 遅延更新
 │
 ├─ 最大エントロピー(両柱の合流)
 │   SAC      : ソフトBellman方程式(自由エネルギー) + 再パラメータ化 + 温度自動調整
 │
 └─ モデル・プランニング
     Dyna     : 実データ + モデル生成データの混合学習
     AlphaZero: MCTS = 方策改善作用素、学習 = 蒸留、というGPI
     MuZero   : 価値等価な潜在モデルでのプランニング
     Dreamer系: 学習した世界モデルの「夢」の中でactor-criticを訓練
```

## 10.2 繰り返し現れた5つの設計原理

個々のアルゴリズムを忘れても、次の5原理は残してほしい。新しい手法に出会ったとき、どの原理の新しい実装かを見抜けるはずである。

1. **教師あり学習への引き戻し。** RLがSGDの前提(i.i.d.・固定分布・固定ラベル)を破ることが不安定性の根源であり、経験再生・並列環境・ターゲットネットワーク・凍結された目標はすべて、学習の各瞬間を教師あり回帰に近づける装置である。
2. **近似された評価器は搾取される(Goodhartの法則)。** $\max$ はノイズを拾い(Double)、アクターはクリティックの誤差の山に登り(TD3)、プランナーはモデルの穴を突き(モデルベースRL)、方策は報酬モデルをハックする(RLHF)。処方箋は一貫して、**悲観化($\min$、保守的推定)、平滑化(目標のノイズ注入)、更新の抑制(遅延更新、KL罰則)**である。
3. **更新の歩幅は分布の距離で測る。** パラメータ空間の小さな一歩が方策分布の大きな一歩でありうる。自然勾配・TRPO・PPO・RLHFのKL罰則・オフラインRLの方策制約は、すべて「分布空間での近接性」の制御である。
4. **バイアスと分散のダイヤルは連続的に回せる。** MC ↔ TD、$n$ステップ、TD($\lambda$)、GAEの $\lambda$、モデル展開の長さ——「実データをどこまで信じ、どこから推定値(ブートストラップ/モデル)に切り替えるか」は常に連続的な設計変数である。
5. **すべてはGPIである。** 評価(価値・報酬モデル・世界モデルの学習)と改善(貪欲化、勾配、探索、蒸留)の交互反復という前作の枠組みは、AlphaZeroからRLHFまで、最先端でもそのまま有効な見取り図である。

## 10.3 開かれた問題

最後に、本テキストの先にある未解決の地平を記す。深層RLの理論的収束保証(deadly triadの一般的解決)は依然として開いている。サンプル効率は教師あり学習に比べ桁違いに悪く、モデルベース化・表現学習・事前学習済みモデルの活用が追究されている。報酬設計の困難(報酬ハッキング、価値のアラインメント)は、AIの能力が上がるほど深刻化する本質的問題である。汎化——訓練環境と異なる環境への頑健性——も、教師あり学習の汎化理論がそのままは通用しない難所である。読者が本テキストで得た「理論の破れと補修」を読む眼は、これらのフロンティアの文献を読む際にそのまま武器になるはずである。

## 10.4 参考文献案内

- R. S. Sutton & A. G. Barto, *Reinforcement Learning: An Introduction* (2nd ed., 2018) — 第II部が関数近似、第III部が発展的話題を扱う。
- 各アルゴリズムの原論文:DQN (Mnih et al., Nature 2015)、Double DQN (van Hasselt et al., 2016)、Rainbow (Hessel et al., 2018)、A3C (Mnih et al., 2016)、GAE (Schulman et al., 2016)、TRPO (Schulman et al., 2015)、PPO (Schulman et al., 2017)、DPG (Silver et al., 2014)、DDPG (Lillicrap et al., 2016)、TD3 (Fujimoto et al., 2018)、SAC (Haarnoja et al., 2018)、AlphaZero (Silver et al., 2018)、MuZero (Schrittwieser et al., 2020)、CQL (Kumar et al., 2020)、InstructGPT/RLHF (Ouyang et al., 2022)、DPO (Rafailov et al., 2023)。
- OpenAI *Spinning Up in Deep RL* — 実装と理論の橋渡しとして優れたオンライン教材。
- S. Levine et al., *Offline Reinforcement Learning: Tutorial, Review, and Perspectives* (2020) — オフラインRLの体系的サーベイ。

---

# 付録A Fisher情報行列と自然勾配の導出

**KLダイバージェンスの2次展開。** パラメトリックな分布族 $p_{\boldsymbol\theta}(x)$ を考え、$D(\Delta) := D_{\mathrm{KL}}(p_{\boldsymbol\theta} \| p_{\boldsymbol\theta + \Delta})$ を $\Delta$ で展開する。

$$
D(\Delta) = \mathbb E_{x \sim p_{\boldsymbol\theta}}\left[ \ln p_{\boldsymbol\theta}(x) - \ln p_{\boldsymbol\theta + \Delta}(x) \right]
$$

$\ln p_{\boldsymbol\theta+\Delta} = \ln p_{\boldsymbol\theta} + \Delta^\top \nabla \ln p_{\boldsymbol\theta} + \frac12 \Delta^\top (\nabla^2 \ln p_{\boldsymbol\theta}) \Delta + O(\Delta^3)$ を代入すると、

$$
D(\Delta) = -\Delta^\top\, \mathbb E[\nabla \ln p_{\boldsymbol\theta}] - \frac12 \Delta^\top\, \mathbb E[\nabla^2 \ln p_{\boldsymbol\theta}]\, \Delta + O(\Delta^3)
$$

**1次の項は消える:** 規格化条件の微分(前作10.4節のベースライン不偏性の証明と同じ計算)により

$$
\mathbb E_{p_{\boldsymbol\theta}}[\nabla \ln p_{\boldsymbol\theta}] = \int p_{\boldsymbol\theta} \frac{\nabla p_{\boldsymbol\theta}}{p_{\boldsymbol\theta}}\, dx = \nabla \int p_{\boldsymbol\theta}\, dx = \nabla 1 = \boldsymbol 0
$$

(KLが $\Delta = 0$ で最小値0をとることからも当然である。)**2次の係数がFisher情報行列:** 同じ恒等式をもう一度微分すると

$$
\boldsymbol 0 = \nabla \mathbb E[\nabla \ln p] = \int \left( \nabla p\, \nabla \ln p^\top + p\, \nabla^2 \ln p \right) dx
= \mathbb E\left[ \nabla \ln p\, \nabla \ln p^\top \right] + \mathbb E\left[ \nabla^2 \ln p \right]
$$

よって $-\mathbb E[\nabla^2 \ln p] = \mathbb E[\nabla \ln p \, \nabla \ln p^\top] =: F(\boldsymbol\theta)$(2つの同値な表式)であり、

$$
D_{\mathrm{KL}}(p_{\boldsymbol\theta} \| p_{\boldsymbol\theta+\Delta}) = \frac12\, \Delta^\top F(\boldsymbol\theta)\, \Delta + O(\Delta^3)
$$

**制約付き最急上昇。** $\max_\Delta \nabla J^\top \Delta$ s.t. $\frac12 \Delta^\top F \Delta \le \epsilon$ のLagrange条件は $\nabla J = \lambda F \Delta$、すなわち

$$
\Delta \propto F^{-1} \nabla J
$$

これが自然勾配である。$F$ が分布多様体のRiemann計量(Fisher–Rao計量)を与え、自然勾配は座標(パラメータ化)の取り方によらない**共変的な**最急上昇方向になる。指数型分布族では自然勾配はニュートン法と密接に関係し、収束が速いことも知られている。

---

# 付録B 再パラメータ化勾配とスコア関数勾配の比較

期待値 $\mathcal J(\boldsymbol\theta) = \mathbb E_{x \sim p_{\boldsymbol\theta}}[f(x)]$ の勾配 $\nabla_{\boldsymbol\theta} \mathcal J$ を推定する2つの方法を整理する。この対比は、REINFORCE/PPO(スコア関数)系とDDPG/SAC(再パラメータ化)系という深層RLの2大流儀の違いの核心である。

**(1) スコア関数勾配(REINFORCE型・前作10.2節)。**

$$
\nabla \mathcal J = \int f(x)\, \nabla p_{\boldsymbol\theta}(x)\, dx = \mathbb E_{x \sim p_{\boldsymbol\theta}}\left[ f(x)\, \nabla \ln p_{\boldsymbol\theta}(x) \right]
$$

- 要件:$p_{\boldsymbol\theta}$ の対数密度が微分可能であればよい。**$f$ は微分不能でもブラックボックスでもよい**(離散の $x$、シミュレータ経由の報酬、など)。
- 弱点:$f$ の値の大きさがそのまま推定量に乗るため**高分散**(ベースラインで軽減)。

**(2) 再パラメータ化勾配(pathwise微分)。** サンプリングを $x = g_{\boldsymbol\theta}(\epsilon)$, $\epsilon \sim q$($q$ は $\boldsymbol\theta$ 非依存)と書き直せるとき、

$$
\nabla \mathcal J = \nabla_{\boldsymbol\theta}\, \mathbb E_{\epsilon \sim q}\left[ f(g_{\boldsymbol\theta}(\epsilon)) \right] = \mathbb E_{\epsilon \sim q}\left[ \nabla_x f(x)\big|_{x = g_{\boldsymbol\theta}(\epsilon)}\; \nabla_{\boldsymbol\theta}\, g_{\boldsymbol\theta}(\epsilon) \right]
$$

- 要件:**$f$ が $x$ について微分可能**で、分布が再パラメータ化可能(ガウス等の連続分布)であること。
- 利点:$f$ の**勾配情報**(どちらに動けば良くなるかの方向)を直接使うため、一般に**大幅に低分散**。

RLの文脈では $f$ の役割を果たすのが $Q$ 関数(クリティック)である。スコア関数系(PPO)は「$Q$(またはアドバンテージ)の**値**で行動の対数確率を重みづける」——評価器の値だけを使う。再パラメータ化系(SAC)およびDPG系(DDPG/TD3)は「$Q$ の**行動微分** $\nabla_a Q$ を方策まで逆伝播する」——評価器の形状(勾配)まで使う。後者は低分散だが、クリティックの勾配の誤りに直接さらされる(6.3節の搾取問題)。この「値を使うか、勾配を使うか」の選択が、分散とバイアス(クリティック誤差への感度)のトレードオフのもう一つの現れになっている。

---

# 付録C 記法一覧(前作からの追加分)

| 記号 | 意味 |
| ---- | ---- |
| $\boldsymbol\theta,\ \boldsymbol w$ | 方策・価値ネットワークのパラメータ |
| $\boldsymbol\theta^-,\ \boldsymbol w^-$ | ターゲットネットワークのパラメータ |
| $\mathcal D$ | リプレイバッファ(または固定データセット) |
| $\tau$ | Polyak平均の追従率(または軌道) |
| $\rho_t(\boldsymbol\theta')$ | 確率比 $\pi_{\boldsymbol\theta'}(a_t \mid s_t)/\pi_{\boldsymbol\theta}(a_t \mid s_t)$ |
| $\hat A_t^{\mathrm{GAE}(\gamma,\lambda)}$ | 一般化アドバンテージ推定量 $\sum_l (\gamma\lambda)^l \delta_{t+l}$ |
| $L_\pi(\pi')$ | 代理目的関数 |
| $D_{\mathrm{KL}}(p \,\|\, q)$ | KLダイバージェンス $\mathbb E_p[\ln(p/q)]$ |
| $F(\boldsymbol\theta)$ | Fisher情報行列 |
| $\mu_{\boldsymbol\theta}(s)$ | 決定論的方策 |
| $\alpha$ | (第7章)エントロピー温度 |
| $\mathcal H(\pi(\cdot \mid s))$ | 方策のエントロピー |
| $Q_{\mathrm{soft}},\ V_{\mathrm{soft}}$ | ソフト価値関数 |
| $Z(s)$ | 分配関数 $\sum_a \exp(Q_{\mathrm{soft}}(s,a)/\alpha)$ |
| $Z(s,a)$ | (第3章)収益の分布(確率変数) |
| $N(s,a)$ | (第8章)MCTSの訪問回数 |
| $\pi_{\mathrm{ref}}$ | (第9章)RLHFの参照方策 |

---

> [!quote] 結び
> 前作の結びで、Bellman方程式を「全体の最適性を局所の整合性で特徴づける原理」と呼んだ。本テキストで見た通り、深層強化学習とはその原理を、収束定理の庇護が及ばない広大な近似の世界へ持ち出す冒険である。そこで得られた教訓——学習を教師あり問題に引き戻せ、近似された評価器の搾取を疑え、歩幅は分布の距離で測れ——は、アルゴリズムの表面が移り変わっても失効しない。理論の背骨(前作)と、それを現実に接ぎ木する技法の体系(本作)。この2つを携えて、読者が原論文と実装の世界へ、そしてまだ誰も解いていない問題へ進むことを願っている。
