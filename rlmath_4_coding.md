---
tags:
  - 強化学習
  - 演習
  - Python
  - 実装
aliases:
  - RL実践コーディング演習
author: Claude Fable 5 (medium)
---
# 強化学習演習 実践コーディング編 — アルゴリズムを自分の環境で動かす

> [!note] シリーズ全体の目次
> - [[強化学習の数理_1_基礎編]] — 古典的RL理論(MDP・Bellman方程式・DP・TD・方策勾配)
> - [[強化学習の数理_2_深層学習編]] — 深層強化学習(DQN・A2C・PPO・SAC等)
> - [[強化学習の数理_3_演習_手計算編]] — 上記2冊の導出を手計算で再構成する演習
> - **[[強化学習の数理_4_演習_実践コーディング編]]**(本稿) — 上記2冊のアルゴリズムをコードで実装する演習

> [!abstract] 本演習テキストについて
> 本テキストは、解説テキスト [[強化学習の数理_1_基礎編]] と [[強化学習の数理_2_深層学習編]] に登場するアルゴリズムを、**標準的なローカル環境(Python + NumPy + Matplotlib のみ)で実装・実行して理論を体感する**ための課題集である。
>
> - 各課題は「ステップ」に分割してあり、**上から順に実装すれば必ず動くところまで到達できる**ように書いてある。骨格コードの `# TODO` を埋めるのが基本作業である。
> - 各課題末尾の `[!check] 確認ポイント` に、正しく実装できたときに得られるはずの**具体的な数値・挙動**を示した。理論値と照合すること。
> - 環境(GridWorld等)もすべて自作する。外部の強化学習ライブラリ・GPU・深層学習フレームワークは不要である。
> - 深層強化学習のうち、ローカル再現が難しい要素(Atari規模のDQN、連続制御のDDPG/TD3/SAC、MCTS/AlphaZero、RLHF)は、**本質を保った縮小版**(課題11・13・14)に置き換えるか、対象外とした。対応関係は各課題の冒頭に記す。
> - 式の導出そのものを確認したくなったら、姉妹編 [[強化学習の数理_3_演習_手計算編]] の対応問題へ。

> [!tip] 準備
> Python 3.9以降で以下が動けばよい。
> ```bash
> pip install numpy matplotlib
> ```
> 各課題は独立した1つの `.py` ファイル(または1つのノートブックセル群)として書ける規模である。乱数は `rng = np.random.default_rng(0)` のように**シードを固定**して再現性を確保すること。

---

## 課題1 2状態MDP:厳密解・反復解・収束レート $\gamma$

**対応理論:** [[強化学習の数理_1_基礎編#第3章 Bellman期待方程式 — 価値の自己無撞着方程式]] 3.3節、[[強化学習の数理_1_基礎編#第5章 Bellman作用素と不動点定理 — 理論の心臓部]]、6.6節の2状態MDP。手計算では [[強化学習の数理_3_演習_手計算編]] 問題4・6・7。

**目的:** Bellman期待方程式が (a) 逆行列で厳密に解け、(b) 作用素の反復でも解け、(c) 反復の誤差が正確に $\gamma^k$ で減衰する、ことを1つの画面で確認する。

### ステップ1 環境の定義

解説テキスト6.6節の2状態MDP($\gamma = 0.9$)を配列で表現する。

```python
import numpy as np
import matplotlib.pyplot as plt

gamma = 0.9
# 状態 0 = s1, 1 = s2 / 行動 0 = stay, 1 = move
R   = np.array([[1.0, 0.0],
                [2.0, 0.0]])        # R[s, a]
NXT = np.array([[0, 1],
                [1, 0]])            # 決定論的遷移: NXT[s, a] = 次状態
```

### ステップ2 方策評価:厳密解

方策 $\pi_0$ =「常に stay」について、$P^{\pi_0}$(2×2行列)と $\boldsymbol r^{\pi_0}$ を作り、

$$\boldsymbol v^{\pi_0} = (I - \gamma P^{\pi_0})^{-1}\boldsymbol r^{\pi_0}$$

を `np.linalg.solve` で計算せよ。

```python
def eval_exact(policy):          # policy[s] = 行動
    P = np.zeros((2, 2)); r = np.zeros(2)
    for s in range(2):
        a = policy[s]
        P[s, NXT[s, a]] = 1.0    # 決定論的遷移
        r[s] = R[s, a]
    return np.linalg.solve(np.eye(2) - gamma * P, r)

print(eval_exact([0, 0]))        # 常にstay
```

### ステップ3 反復的方策評価と収束レートの実測

同じ方策を $v_0 = \boldsymbol 0$ から $v_{k+1} = T^{\pi}v_k$ で解き、各反復の誤差 $\|v_k - v^\pi\|_\infty$ を記録して**片対数プロット**せよ。隣接誤差の比 `err[k+1]/err[k]` も表示すること。

### ステップ4 価値反復で最適方策へ

$v_{k+1}(s) = \max_a[R(s,a) + \gamma\,v_k(\text{NXT}[s,a])]$ を実装し、$V^*$ と、それに貪欲な方策を出力せよ。ステップ3と同様に $\|v_k - V^*\|_\infty$ の減衰も記録する。

> [!check] 確認ポイント
> - 厳密解:「常に stay」で $V = (10, 20)$、最適で $V^* = (18, 20)$、最適方策は $\pi^*(s_1) = \text{move}$, $\pi^*(s_2) = \text{stay}$(解説テキスト6.6節の手計算と一致)。
> - 誤差の片対数プロットが**直線**になり、隣接誤差比が $0.900$ にほぼ一致する(数反復目以降)。定理5.5の幾何収束レート $\gamma^k$ の実証である。
> - `gamma = 0.99` に変えると直線の傾きが緩くなり、同じ精度に要する反復回数が約10倍になる(手計算編 問6.5)。

---

## 課題2 4×4 GridWorldで方策反復と価値反復

**対応理論:** [[強化学習の数理_1_基礎編#第6章 動的計画法 — モデルが既知のときの厳密解法]]。

**目的:** 状態数16の小さな世界で、GPIの2つの極(方策反復・価値反復)を両方実装し、同じ最適解に到達することを確認する。

### ステップ1 環境:4×4グリッド

- 状態 0〜15(左上0、右下15を行優先で番号づけ)。**0 と 15 が終端**。
- 行動:上/下/左/右。壁にぶつかるとその場に留まる。
- 報酬:終端以外での全遷移で $-1$(「早くゴールせよ」)。$\gamma = 1$(エピソード的タスクなので許される)。

```python
N = 16; TERM = (0, 15); ACTIONS = range(4)

def step(s, a):
    if s in TERM: return s, 0.0
    r, c = divmod(s, 4)
    if   a == 0: r = max(r - 1, 0)
    elif a == 1: r = min(r + 1, 3)
    elif a == 2: c = max(c - 1, 0)
    else:        c = min(c + 1, 3)
    return r * 4 + c, -1.0
```

### ステップ2 一様ランダム方策の反復的方策評価

$\pi(a|s) = 1/4$ を、同期更新で $\max$ 差分が $10^{-8}$ を切るまで評価し、`V.reshape(4,4)` を丸めて表示せよ。

### ステップ3 方策反復

評価(ステップ2の関数を任意の方策に一般化)と貪欲化を交互に回し、方策が変化しなくなるまで反復。何回の改善で停止したかを表示せよ。

### ステップ4 価値反復

$v_{k+1}(s) = \max_a[r + v_k(s')]$ を回し、方策反復と同じ $V^*$・同じ貪欲方策に達することを確認せよ。

> [!check] 確認ポイント
> - ランダム方策の価値(収束値、丸めて整数):
>   ```
>   [[  0. -14. -20. -22.]
>    [-14. -18. -20. -20.]
>    [-20. -20. -18. -14.]
>    [-22. -20. -14.   0.]]
>   ```
>   対称性(180°回転で不変)が出ていれば実装はほぼ正しい。
> - 最適価値:
>   ```
>   [[ 0. -1. -2. -3.]
>    [-1. -2. -3. -2.]
>    [-2. -3. -2. -1.]
>    [-3. -2. -1.  0.]]
>   ```
>   =「最寄りの終端までの最短手数の符号反転」。
> - 方策反復は**ごく少数回**(高々3回程度)の改善で停止する。「改善停止 ⟺ 最適到達」(手計算編 問8.4)の実演。

---

## 課題3 モンテカルロ方策評価:サンプル平均は $1/\sqrt N$ で収束する

**対応理論:** [[強化学習の数理_1_基礎編#第7章 モンテカルロ法 — モデルフリー学習への第一歩]]。手計算では [[強化学習の数理_3_演習_手計算編]] 問題9.1。

**目的:** モデル $(P, R)$ を**一切使わず**、軌道サンプルだけから価値を推定できること、およびその統計的収束の速さを体感する。

### ステップ1 環境:5状態ランダムウォーク

状態 $1, \dots, 5$ を一列に並べ、両端の外($0$ と $6$)が終端。方策は「左右へ確率 $1/2$ ずつ」。右端終端に到達したときのみ報酬 $+1$、それ以外は $0$。$\gamma = 1$。開始状態は中央の $3$。

**真の価値は $V^\pi(k) = k/6$**($k = 1, \dots, 5$)であることが知られている(右端に先に到達する確率。対称ランダムウォークの吸収確率)。

### ステップ2 first-visit MC

エピソードを生成する関数 `episode(rng)`(状態列と報酬列を返す)を書き、first-visit MC:

```python
V = np.zeros(7); returns_count = np.zeros(7)
for ep in range(N_EPISODES):
    states, rewards = episode(rng)
    G = 0.0; visited = set()
    for t in reversed(range(len(states))):
        G = rewards[t] + G                    # gamma = 1
        s = states[t]
        # TODO: first-visit判定(そのエピソード内で s の最初の訪問のときだけ)
        #       逐次平均 V[s] += (G - V[s]) / returns_count[s] で更新
```

を実装せよ(逆順に回して first-visit を扱うときは「その時刻より前に同じ状態が現れないか」で判定する。素直に前から2パスで処理してもよい)。

### ステップ3 収束の測定

エピソード数 $N \in \{10, 10^2, 10^3, 10^4\}$ について、推定値と真値のRMS誤差 $\sqrt{\frac15\sum_k(\hat V(k) - k/6)^2}$ を(乱数シードを変えて20回平均して)計算し、**両対数プロット**せよ。

> [!check] 確認ポイント
> - $N = 10^4$ で $\hat V \approx (0.167, 0.333, 0.500, 0.667, 0.833)$ に近づく(各成分の誤差が数%以内)。
> - 両対数プロットの傾きが約 $-1/2$、すなわち誤差 $\propto 1/\sqrt N$。大数の法則・中心極限定理どおりのモンテカルロの収束速度である。

---

## 課題4 TD(0) vs MC:バイアス・分散トレードオフを可視化する

**対応理論:** [[強化学習の数理_1_基礎編#第8章 時間差分 (TD) 学習 — SARSAとQ学習]] 8.1–8.2節。

**目的:** 同じ課題3のランダムウォークで、TD(0) が (i) エピソード途中でも逐次更新できること、(ii) 序盤の学習がMCより速いこと(低分散の恩恵)を実測する。

### ステップ1 TD(0) の実装

初期値 $\hat V(s) = 0.5$(全状態)から、遷移 $(S_t, R_{t+1}, S_{t+1})$ ごとに

$$\hat V(S_t) \leftarrow \hat V(S_t) + \alpha\,[R_{t+1} + \hat V(S_{t+1}) - \hat V(S_t)]$$

を実装せよ(終端では $\hat V(S_{t+1}) = 0$ とする)。$\alpha = 0.1$。

### ステップ2 学習曲線の比較

MC(定数 $\alpha = 0.02$ の(7.1)型更新に変更したもの)とTD(0)($\alpha = 0.1$)について、エピソード数 0〜200 にわたるRMS誤差(対真値 $k/6$)の推移を、シード100本の平均としてプロットせよ。

### ステップ3 ステップサイズの影響

TD(0) の $\alpha \in \{0.05, 0.1, 0.2\}$ で同じ曲線を重ね描きし、「大きい $\alpha$ は序盤速いが到達精度が悪い(揺らぎが残る)」ことを確認せよ。Robbins–Monro条件($\sum\alpha_k = \infty, \sum\alpha_k^2 < \infty$;定数 $\alpha$ はこれを満たさない)との関係を考察の代わりに`[!note]`としてノートに書き留めよ:**定数 $\alpha$ では誤差はゼロにならず、$\alpha$ に比例した揺らぎの床に達して止まる。**

> [!check] 確認ポイント
> - 両曲線とも単調に誤差が減り、**最初の数十エピソードではTDがMCより明確に速い**(この課題設定はTD有利の古典例である)。
> - $\alpha$ を大きくすると立ち上がりが速くなる代わりに、終盤の誤差の床が高くなる。
> - TDはエピソード終了を待たずに毎ステップ更新している(コードの構造がそれを示している)ことを確認せよ——継続的タスクにも使える形である。

---

## 課題5 SARSA vs Q学習:崖歩き(Cliff Walking)

**対応理論:** [[強化学習の数理_1_基礎編#第8章 時間差分 (TD) 学習 — SARSAとQ学習]] 8.4–8.5節。手計算では [[強化学習の数理_3_演習_手計算編]] 問題9.5。

**目的:** 方策オン(SARSA)と方策オフ(Q学習)の違いが、**学習される経路の質的な違い**として現れる有名な現象を再現する。

### ステップ1 環境:4×12の崖グリッド

- 4行×12列。左下がスタートS、右下がゴールG。S と G の間の最下行(10マス)が**崖**。
- 報酬:通常の遷移 $-1$、崖に落ちると $-100$ でスタートへ戻る。ゴールで終了。$\gamma = 1$。

### ステップ2 $\varepsilon$-貪欲方策と2つの学習則

$\varepsilon = 0.1$(固定)、$\alpha = 0.5$ で、

- SARSA:$Q(S_t,A_t) \mathrel{+}= \alpha[R + \gamma Q(S_{t+1}, A_{t+1}) - Q(S_t,A_t)]$($A_{t+1}$ は実際に選んだ次の行動)
- Q学習:$Q(S_t,A_t) \mathrel{+}= \alpha[R + \gamma\max_{a'}Q(S_{t+1}, a') - Q(S_t,A_t)]$

をそれぞれ500エピソード学習させよ。

### ステップ3 比較

(a) エピソードごとの収益(報酬和)の移動平均を両者で重ね描き。(b) 学習後の貪欲方策で1エピソード走らせ、通る経路をグリッド上に印字せよ。

> [!check] 確認ポイント
> - 学習後の貪欲経路:**Q学習は崖すれすれの最短路**、**SARSAは崖から1〜2行離れた安全路**を通る。
> - 学習**中**の平均収益(ε-探索込み)は**SARSAの方が高い**(Q学習は最適経路を学ぶが、探索で崖に落ちる事故を織り込まない)。
> - 理由を自分の言葉で1段落書け:SARSAは「探索込みの自分自身($\varepsilon$-貪欲方策)の $Q^\pi$」を、Q学習は「探索とは無関係の $Q^*$」を推定している——目標値の $A_{t+1}$ と $\max_{a'}$ の一字の違いがこの差を生む(手計算編 問9.5 の数値例の大規模版)。

---

## 課題6 過大評価バイアスとDouble Q学習

**対応理論:** [[強化学習の数理_1_基礎編#第8章 時間差分 (TD) 学習 — SARSAとQ学習]] 8.5節の警告、[[強化学習の数理_2_深層学習編#第3章 DQNの系譜 — Rainbowに至る改良の解剖]] 3.1節。手計算では [[強化学習の数理_3_演習_手計算編]] 問題9.6・12。**Double DQN・TD3の $\min$ 悲観化の本質はこの表形式実験に含まれている(縮小版)。**

**目的:** $\max$ 演算がノイズを系統的に拾う現象を、最小のMDPで再現し、Double推定で消えることを確認する。

### ステップ1 環境:バイアス検出用の2状態MDP

- 状態A(開始)。行動は「右」(報酬0で即終端)と「左」(報酬0で状態Bへ)。
- 状態B。行動は**10本**あり、どれを選んでも終端。報酬は $\mathcal N(-0.1,\ 1.0)$ からのサンプル。
- つまり**左へ行く真の価値は $-0.1 < 0$ で、常に「右」が最適**。しかしBの10行動のQ推定ノイズを $\max$ が拾うと、左が良く見えてしまう。

### ステップ2 Q学習とDouble Q学習

$\varepsilon = 0.1$、$\alpha = 0.1$、$\gamma = 1$。Double Q学習は2つの表 $Q_1, Q_2$ を持ち、毎回コイントスでどちらを更新するか選び、

$$Q_1(S,A) \mathrel{+}= \alpha\big[R + \gamma\,Q_2\big(S',\ \arg\max_{a'}Q_1(S',a')\big) - Q_1(S,A)\big]$$

(**選択は $Q_1$、評価は $Q_2$**。$Q_2$ 更新時は役割を入れ替え)。行動選択は $Q_1 + Q_2$ に対する $\varepsilon$-貪欲。

### ステップ3 計測

300エピソード × 1000シードで、「状態Aで左を選んだ割合」のエピソード推移を両手法で重ね描きせよ。参考線として、最適下の水準 $\varepsilon/2 = 5\%$(探索による左選択の下限)を引くこと。

> [!check] 確認ポイント
> - **Q学習は序盤、左選択率が大きく跳ね上がる**(ピークで数十%)。真の価値が負なのに、10本の行動のノイズの $\max$ が正に見えるため。学習が進むと徐々に $5\%$ 付近へ降りてくるが遅い。
> - **Double Q学習はほぼ最初から $5\%$ 付近に張り付く。**
> - Bの行動数を10→2に減らす/報酬の分散を1→0.1に減らすと、Q学習のバイアスの山が小さくなることを確認せよ(バイアス $\sim \sigma\sqrt{2\ln|\mathcal A|}$ の定性的検証。[[強化学習の数理_2_深層学習編]] 3.1節)。

---

## 課題7 線形関数近似:semi-gradient TD と状態集約

**対応理論:** [[強化学習の数理_1_基礎編#第9章 関数近似への入り口 — 表形式の限界を超える]]。手計算では [[強化学習の数理_3_演習_手計算編]] 問題11.3。

**目的:** 表を捨てて $\hat v = \boldsymbol w^\top\boldsymbol\phi(s)$ に移行しても同じ更新則が動くこと、そして**表現力の限界内の解**に収束すること(誤差がゼロにならない)を見る。

### ステップ1 環境と特徴量

課題3のランダムウォークを**19状態**版に拡張する(状態 $1..19$、両端の外が終端、右端で $+1$・左端で $-1$、開始は中央10。真値は $V(k) = (k-10)/10$)。特徴量は**状態集約**:19状態を「1–5 / 6–10 / 11–15 / 16–19」の4グループに分け、$\boldsymbol\phi(s) \in \mathbb R^4$ を所属グループのone-hotとする。

### ステップ2 semi-gradient TD(0)

$$\boldsymbol w \leftarrow \boldsymbol w + \alpha\,\delta_t\,\boldsymbol\phi(S_t),\qquad \delta_t = R_{t+1} + \gamma\,\boldsymbol w^\top\boldsymbol\phi(S_{t+1}) - \boldsymbol w^\top\boldsymbol\phi(S_t)$$

を実装し($\gamma = 1$、$\alpha = 0.05$、10000エピソード、$\alpha$ は後半 $0.005$ に落とす)、学習後の $\hat v(s)$ を真値と重ね描きせよ。

### ステップ3 観察

(a) $\hat v$ が**階段状**(グループ内で一定)になること、(b) 各段がそのグループ内の真値の(訪問頻度重みつき)平均付近に来ること、(c) 特徴量を19次元one-hot(=表形式)に差し替えると誤差が消えること、を確認せよ。

> [!check] 確認ポイント
> - 階段の4段はおよそ $(-0.7, -0.25, +0.25, +0.7)$ 前後(グループ割りが非対称なので端の段はやや偏る)。
> - one-hot版はほぼ真値の直線 $(k-10)/10$ を再現する。表形式が線形近似の特殊ケース($\boldsymbol\phi = \boldsymbol e_s$)であることのコードによる証明。
> - **誤差がゼロにならないのはバグではない**:近似族の表現力の限界内での最良近似(の $\frac{1}{1-\gamma}$ 倍以内)に収束している(9.3節の結果)。

---

## 課題8 Deadly Triad:Bairdの反例で発散を起こす

**対応理論:** [[強化学習の数理_1_基礎編#第9章 関数近似への入り口 — 表形式の限界を超える]] 9.4節。**DQNの不安定性([[強化学習の数理_2_深層学習編]] 第2章)の根っこを、線形・7状態というローカルで完全再現できる最小例で見る課題である。**

**目的:** 「関数近似+ブートストラップ+方策オフ」が同時に揃うと、線形近似ですら価値推定が**発散**することを自分のマシンで起こす。

### ステップ1 Bairdの星型MDP

- 状態 $0..6$。**目標方策**:常に行動 solid をとる。solid は必ず状態6へ遷移する。報酬は常に0。
- したがって真値は自明に $V^\pi \equiv 0$。
- **挙動方策**:一様に全状態を訪れる(簡単のため、毎ステップ状態を一様ランダムに選んで1遷移だけ観測する形でよい)。ここに**方策オフ性**(挙動分布 ≠ 目標方策の訪問分布)がある。
- 特徴量($d = 8$、過剰パラメータ化):

```python
Phi = np.zeros((7, 8))
for s in range(6):
    Phi[s, s] = 2.0; Phi[s, 7] = 1.0
Phi[6, 6] = 1.0; Phi[6, 7] = 2.0
w = np.ones(8); w[6] = 10.0        # 慣例的な初期値
gamma = 0.99
```

### ステップ2 方策オフ semi-gradient TD(0)

毎ステップ:状態 $s$ を一様に選び、目標方策に従う遷移($s' = 6$、$r = 0$)について

$$\delta = 0 + \gamma\,\boldsymbol\phi(6)^\top\boldsymbol w - \boldsymbol\phi(s)^\top\boldsymbol w,\qquad \boldsymbol w \mathrel{+}= \alpha\,\rho\,\delta\,\boldsymbol\phi(s)$$

を実行($\alpha = 0.01$。$\rho$ は重点サンプリング比;この簡略設定では一様な状態選択を使うので $\rho = 1$ とした素朴版でもよいし、元の設定に忠実に $\rho = 7$ を掛けてもよい——どちらでも発散する)。$\|\boldsymbol w\|$ を1000ステップ記録せよ。

### ステップ3 発散の確認と「治療」

(a) $\|\boldsymbol w\|$ の推移を片対数プロットし、指数的発散を確認する。(b) 次の**どれか1つ**を外すと発散が止まることを実験で確かめよ:
- ブートストラップをやめる(目標を真の収益 $0$、すなわちMC目標にする)
- 方策オフをやめる(状態を目標方策の訪問分布=ほぼ状態6のみ、からサンプルする)
- 関数近似をやめる($\boldsymbol\phi = \boldsymbol e_s$ の7次元one-hotにする)

> [!check] 確認ポイント
> - 素朴な方策オフ semi-gradient TD では $\|\boldsymbol w\|$ が**指数的に爆発**する(数百ステップで $10^3$、1000ステップで $10^8$ 規模。真値はゼロなのに!)。
> - 3要素のどれを外しても発散が止まる——**deadly triad は「3つ同時」のときだけ発火する**ことの実験的確認。
> - この現象こそ、DQNが経験再生(分布の制御)とターゲットネットワーク(ブートストラップの自己言及の切断)を必要とした理由である。次の課題11でその処方箋を入れる。

---

## 課題9 REINFORCE とベースラインによる分散低減

**対応理論:** [[強化学習の数理_1_基礎編#第10章 方策勾配法 — 方策を直接最適化する]]。手計算では [[強化学習の数理_3_演習_手計算編]] 問題10。

**目的:** 方策を直接パラメータ化して勾配上昇で学習できること、そしてベースラインが**期待値を変えずに分散だけを下げる**ことを数値で確認する。

### ステップ1 環境:短い回廊

- 4状態の一列 $0,1,2,3$(3が終端ゴール、報酬 $+1$;それ以外の遷移は報酬 $0$)。開始は0。行動は左/右(端では留まる)。$\gamma = 0.9$。
- **ひねり**:状態1だけ左右の効果が**反転**する(右を選ぶと左へ動く)。こうすると「常に右」は最適でなく、**確率的方策に意味が生まれる**……のだが、状態を区別できる表形式softmaxなら各状態で決定論に収束できる。まずは素直に解かせる。

### ステップ2 表形式softmax方策とREINFORCE

方策は各状態独立のsoftmax:$\pi_\theta(a|s) \propto e^{\theta_{s,a}}$($\theta \in \mathbb R^{4\times2}$)。スコア関数は手計算編 問10.4 の one-hot版:

$$\nabla_{\theta_{s,\cdot}}\ln\pi(a|s) = \boldsymbol e_a - \pi(\cdot|s)$$

エピソードを生成し、各時刻について $\theta_{S_t,\cdot} \mathrel{+}= \alpha\,\gamma^t\,G_t\,(\boldsymbol e_{A_t} - \pi(\cdot|S_t))$ を実装せよ($\alpha = 0.1$、2000エピソード)。

### ステップ3 ベースラインの追加と分散の測定

(a) 状態価値の推定 $b(s) = \hat V(s)$ を、MC更新 $\hat V(S_t) \mathrel{+}= \alpha_v(G_t - \hat V(S_t))$ で並走させ、重みを $G_t - b(S_t)$ に置き換えた版を作る。
(b) **勾配推定の分散を直接測る**:学習を途中(たとえば200エピソード時点)で凍結し、その方策のまま500エピソード分の「1エピソードあたりの勾配推定量」を集め、成分ごとの分散の総和をベースラインあり/なしで比較せよ。
(c) 学習曲線(エピソード収益の移動平均)を、あり/なし × シード20本平均で重ね描きせよ。

> [!check] 確認ポイント
> - どちらの版も最終的にゴール到達方策(平均収益がほぼ理論上限)へ収束する——ベースラインは**不偏性を壊さない**(手計算編 問10.3 の実験版)。
> - 凍結時点の勾配分散は、ベースラインありが**明確に小さい**(設定次第で数分の一)。
> - 学習曲線はベースラインありの方が滑らかで速い。

---

## 課題10 アクター・クリティック:TD誤差を共有する

**対応理論:** [[強化学習の数理_1_基礎編#第10章 方策勾配法 — 方策を直接最適化する]] 10.5節、[[強化学習の数理_2_深層学習編#第4章 深層方策勾配 (I) — A2C/A3C と一般化アドバンテージ推定 (GAE)]] 4.1節。**A2Cの縮小版(並列環境なし・表形式)である。**

**目的:** MC収益 $G_t$ をTD目標に置き換えることで、エピソード終了を待たない**完全オンライン**の方策学習が動くことを確認する。

### ステップ1 実装

課題9と同じ環境・同じsoftmax方策で、1ステップごとに:

```python
delta = r + gamma * V[s_next] * (not done) - V[s]     # TD誤差
V[s]        += alpha_w * delta                         # クリティック
theta[s, :] += alpha_t * I * delta * (onehot(a) - pi)  # アクター
I *= gamma                                             # γ^t 因子
```

を実行せよ($\alpha_w = 0.1$, $\alpha_t = 0.1$)。

### ステップ2 比較

課題9のREINFORCE(ベースラインあり)と学習曲線を重ね、(a) 序盤の立ち上がり、(b) 曲線の滑らかさ、を比較せよ。

> [!check] 確認ポイント
> - アクター・クリティックも同じ最適方策に到達する。
> - 更新がエピソード内で毎ステップ走っている(コード構造)。REINFORCEはエピソード末尾まで更新できない。
> - 同じ $\delta_t$ が2行(アクターとクリティック)で共有されている——「予想より良ければその行動を強化」という10.5節の記述がコード2行に対応することを確認せよ。

---

## 課題11 ミニDQN:リプレイバッファとターゲットネットの効果測定

**対応理論:** [[強化学習の数理_2_深層学習編#第2章 DQN — 深層Q学習と不安定性への最初の処方箋]]。**Atari規模のDQNのローカル再現は対象外とし、その2大発明(経験再生・ターゲットネットワーク)の効果を、線形関数近似 + 崖歩き環境で切り分けて測る縮小版とする。** 手計算では [[強化学習の数理_3_演習_手計算編]] 問題11。

**目的:** 「素朴な近似Q学習」に経験再生とターゲットネットを1つずつ足し、学習の安定性がどう変わるかをアブレーション(部品の抜き差し)で観察する。

### ステップ1 関数近似つきQ学習の骨格

課題5の崖歩き環境を使う。$Q(s, a) = \boldsymbol w_a^\top\boldsymbol\phi(s)$、特徴 $\boldsymbol\phi(s)$ は**タイルコーディング風の粗い集約**:状態の (行, 列) をそれぞれ2マス幅で区切ったone-hotの連結($2 + 6 = 8$ 次元)+バイアス項。この「近すぎる状態が同じ特徴を共有する」設計は、汎化と干渉を意図的に持ち込むためのものである。

### ステップ2 4条件の比較実験

次の4条件を、同一シード集合(20本)で各500エピソード学習させ、エピソード収益の移動平均を4本重ね描きせよ。

1. **素朴版**:遷移を観測するたび、その1遷移で semi-gradient Q学習更新。
2. **+リプレイ**:遷移をバッファ(容量5000)に貯め、毎ステップ、バッファから32件を一様サンプルしてミニバッチ更新。
3. **+ターゲット**:目標値の計算に凍結パラメータ $\boldsymbol w^-$ を使い、200更新ごとに $\boldsymbol w^- \leftarrow \boldsymbol w$。
4. **+両方**(=ミニDQN)。

```python
# 目標値(条件3・4)
y = r + gamma * (not done) * np.max(phi_next @ W_target.T)
# 更新はつねにオンライン側 W に対して:
W[a] += alpha * (y - phi @ W[a]) * phi
```

### ステップ3 観察

(a) 学習曲線の平均と、(b) シード間の**ばらつき**(帯として描く)を比較せよ。(c) 素朴版で崩壊(収益が一度上がってから激しく落ちる)が起きるシードの割合を数えよ。

> [!check] 確認ポイント
> - 素朴版は動くこともあるが**シードによって不安定**(振動・崩壊が最も多い)。両方入り(条件4)が最も安定して高収益に到達する。
> - リプレイだけ・ターゲットだけでも部分的に改善する——2つの発明が**別々の病理**(データ相関 / 動く目標)への処方箋であることの切り分け実験。
> - 効果の出方は環境・特徴量・ハイパーパラメータに依存する(理論保証がない領域に入っている!)。「どの条件がどれくらい安定か」を自分の数字で語れるようにしておくこと。

---

## 課題12 GAE:λ で分散とバイアスのダイヤルを回す

**対応理論:** [[強化学習の数理_2_深層学習編#第4章 深層方策勾配 (I) — A2C/A3C と一般化アドバンテージ推定 (GAE)]] 4.3節。手計算では [[強化学習の数理_3_演習_手計算編]] 問題14。

**目的:** GAEの後ろ向き再帰を実装し、$\lambda$ がアドバンテージ推定量の分散(とバイアス)を連続的に動かすことを実測する。

### ステップ1 実装と単体テスト

```python
def gae(deltas, gamma, lam):
    A = np.zeros(len(deltas)); acc = 0.0
    for t in reversed(range(len(deltas))):
        acc = deltas[t] + gamma * lam * acc
        A[t] = acc
    return A
```

**単体テスト**:`deltas = [1, -0.5, 2]`, `gamma=0.9`, `lam=0.5` で `A = [1.18, 0.4, 2.0]` になること(手計算編 問14.4 の答えと一致)を `assert` せよ。

### ステップ2 分散の実測

課題3の19状態ランダムウォーク(課題7版)で、方策は一様ランダムに固定。クリティックは**わざと不正確に**する:真値 $V(k) = (k-10)/10$ に一様ノイズ $\pm 0.3$ を加えて固定した $\hat V$ を使う。

1000エピソードを生成し、開始状態のアドバンテージ推定 $\hat A_0^{\mathrm{GAE}(\lambda)}$ を $\lambda \in \{0, 0.5, 0.9, 1.0\}$ で計算して、その**標本平均と標本分散**を表にせよ。

### ステップ3 読み取り

- 分散が $\lambda$ とともに単調増加すること($\lambda = 0$ は $\delta_0$ のみ=1遷移分のノイズ、$\lambda = 1$ は $G_0 - \hat V(s_0)$=全ステップのノイズ)。
- 平均(真のアドバンテージは0のはず。方策で条件づけない開始時点の平均アドバンテージは0)からのずれ=バイアスが、$\lambda$ が小さいほど($\hat V$ のノイズを通じて)大きく残ること。

> [!check] 確認ポイント
> - 単体テストが通ること($[1.18, 0.4, 2.0]$)。
> - 分散:$\lambda = 0$ が最小、$\lambda = 1$ が最大で、間が単調につながる。
> - 平均のずれ:$\lambda = 1$ でほぼ0(不偏:$\hat V$ は $\hat A$ の式で $-\hat V(s_0)$ としてしか効かず、開始状態固定なら定数シフト)、$\lambda$ が小さいほどクリティックの誤差が混入する。**「実データをどこまで信じ、どこからブートストラップに切り替えるか」のダイヤル**が数字として見える。

---

## 課題13 ミニPPO:クリップは「行きすぎ」だけを止める

**対応理論:** [[強化学習の数理_2_深層学習編#第5章 深層方策勾配 (II) — 性能差分補題・自然勾配・TRPO・PPO]] 5.6節。**TRPO(共役勾配・直線探索)はローカル演習の対象外とし、その思想の1次法への蒸留であるPPOのクリップ機構だけを、表形式softmax方策で抽出して確かめる。** 手計算では [[強化学習の数理_3_演習_手計算編]] 問題15・16。

**目的:** 「同じバッチで何エポックも更新を回す」というPPOの使い方が、クリップなしでは壊れ、クリップありでは安全になることを最小構成で見る。

### ステップ1 クリップ目的関数の実装と単体テスト

```python
def clipped_obj(rho, A, eps=0.2):
    return np.minimum(rho * A, np.clip(rho, 1 - eps, 1 + eps) * A)
```

**単体テスト**(手計算編 問16 の4ケース):`(rho, A) = (1.5, 1) → 1.2`、`(0.7, 1) → 0.7`、`(0.7, -1) → -0.8`、`(1.5, -1) → -1.5` を `assert` せよ。

### ステップ2 バッチ再利用の実験

課題9の回廊環境・表形式softmax方策で、次の学習ループを組む:

1. 現在の方策 $\pi_{\text{old}}$ でエピソードを32本収集(状態・行動・$\hat A_t$。$\hat A$ は課題12のGAE、$\lambda = 0.95$、クリティックはMC更新で並走)。
2. 同じバッチで **$K$ エポック**、目的
   $$L(\theta) = \frac1N\sum_t \text{clipped\_obj}\big(\rho_t(\theta),\ \hat A_t\big),\qquad \rho_t(\theta) = \frac{\pi_\theta(a_t|s_t)}{\pi_{\text{old}}(a_t|s_t)}$$
   の勾配上昇($\rho_t$ の $\theta$ 微分は $\rho_t\,\nabla\ln\pi_\theta$;クリップ発動時は勾配0にするのを忘れずに)。
3. $\pi_{\text{old}} \leftarrow \pi_\theta$ として1へ。

### ステップ3 比較

$K = 10$ に固定し、(a) クリップあり、(b) クリップなし(素の $\rho_t\hat A_t$)、の学習曲線と、各更新後の平均確率比 $\bar\rho$ の推移を重ね描きせよ。

> [!check] 確認ポイント
> - 単体テストが通ること。
> - **クリップなし + 多エポック**では、$\bar\rho$ が 1 から大きく離れ(2〜数倍)、学習が振動・崩壊しやすい(古いデータで推定した勾配が新しい方策ではもはや勾配でない——5.1節の病理)。
> - **クリップあり**では $\bar\rho$ が概ね $[1-\epsilon, 1+\epsilon]$ 近傍に留まり、同じ $K = 10$ でも学習が安定する。
> - $\epsilon$ を 0.05 / 0.2 / 0.5 と振ると、「保守的で遅い ↔ 大胆で不安定」のトレードオフが動くことを確認せよ。

---

## 課題14 ソフト価値反復:log-sum-exp と零温度極限

**対応理論:** [[強化学習の数理_2_深層学習編#第7章 最大エントロピー強化学習 — ソフトBellman方程式とSAC]]。**SAC本体(連続行動・再パラメータ化・DNN)は対象外とし、その理論的核であるソフトBellman方程式を表形式で解く縮小版とする。** 手計算では [[強化学習の数理_3_演習_手計算編]] 問題19。

**目的:** Bellman最適作用素の $\max$ を log-sum-exp に置き換えても反復が収束すること(縮小性の存続)、温度 $\alpha \to 0$ で通常の価値反復に連続的に接続することを確認する。

### ステップ1 数値的に安定な log-sum-exp

素朴に `alpha * np.log(np.sum(np.exp(Q / alpha)))` と書くと、$\alpha$ が小さいとき $e^{Q/\alpha}$ が**オーバーフロー**する。最大値をくくり出す標準手法

$$\mathrm{lse}_\alpha(Q) = \max_a Q_a + \alpha\ln\sum_a e^{(Q_a - \max_b Q_b)/\alpha}$$

を実装し、`Q = [2, 0], alpha = 0.001` で落ちずに $\approx 2.0$ を返すことを確認せよ(この式変形自体、「lse は max + 補正項」という構造の証明にもなっている)。

### ステップ2 ソフト価値反復

課題1の2状態MDP($\gamma = 0.9$)で、

$$Q_{k+1}(s, a) = R(s,a) + \gamma\,V_k(s'),\qquad V_k(s) = \mathrm{lse}_\alpha\big(Q_k(s, \cdot)\big)$$

を収束まで反復し、収束後の $V_{\mathrm{soft}}$ と Boltzmann方策 $\pi^*(a|s) = e^{(Q(s,a) - V(s))/\alpha}$ を出力せよ。$\alpha \in \{1.0, 0.5, 0.1, 0.01\}$ で実行する。

### ステップ3 観察

(a) $V_{\mathrm{soft}}$ と $\pi^*(s_1)$ の $\alpha$ 依存性を表にする。(b) 各 $\alpha$ で誤差の片対数プロットを描き、収束レートが依然として $\gamma = 0.9$ であること(縮小性の存続)を確認する。(c) 方策のエントロピー $\mathcal H(\pi^*(\cdot|s_1))$ を $\alpha$ に対してプロットし、「温度を下げると凝縮する」ことを見る。

> [!check] 確認ポイント
> - 収束値(小数3桁):
>
> | $\alpha$ | $V_{\mathrm{soft}}(s_1)$ | $V_{\mathrm{soft}}(s_2)$ | $\pi^*(\text{move} \mid s_1)$ |
> | ---- | ---- | ---- | ---- |
> | 1.0 | 18.843 | 20.344 | 0.587 |
> | 0.5 | 18.113 | 20.003 | 0.803 |
> | 0.1 | 18.000 | 20.000 | 1.000 |
> | 0.01 | 18.000 | 20.000 | 1.000 |
>
> - $\alpha \to 0$ で $V_{\mathrm{soft}} \to V^* = (18, 20)$(課題1の値)、方策は貪欲($s_1$ で move)へ凝縮する。$V_{\mathrm{soft}} \ge V^*$(エントロピーボーナス分だけ高い)であることにも注意。
> - どの $\alpha$ でも誤差比は $\approx 0.9$:**「$\max$ → lse」の置換で収束理論が丸ごと生き残る**(手計算編 問19.4)ことの数値的裏づけ。
> - ステップ1の安定化を入れないと $\alpha = 0.01$ でオーバーフローすることを、あえて素朴版でも走らせて確認しておくとよい。

---

## 対象外としたトピックと、その本質の所在

以下は標準的なローカル環境での忠実な再現が難しい(計算資源・実装量・外部依存の点で)ため課題化していない。ただし、その**理論的な本質**は本テキストの縮小版課題に含まれている。

| 対象外のトピック | 本質を体験できる課題 |
| ---- | ---- |
| Atari規模のDQN・Rainbow | 課題11(経験再生・ターゲットネット)+ 課題6(Double)+ 課題8(不安定性の根源) |
| TRPO(自然勾配・共役勾配) | 課題13(信頼領域思想のクリップへの蒸留)+ 手計算編 問17(Fisher行列) |
| DDPG / TD3 / SAC(連続行動・DNN) | 課題6($\min$系悲観化の動機)+ 課題14(ソフトBellman方程式)+ 手計算編 問18(DPGの連鎖律) |
| MCTS / AlphaZero / MuZero | 課題2(プランニング=DPの原型。MCTSは「改善作用素」としてのGPI部品:[[強化学習の数理_2_深層学習編]] 8.3節) |
| RLHF / DPO | 手計算編 問20(Bradley–Terryの勾配とKL正則化閉形式が理論の全部品) |

> [!quote] 結び
> ここまでの14課題で、読者の手元には「Bellman方程式を解く」ことの実装的な意味——厳密に(課題1–2)、サンプルで(3–6)、近似の上で(7–8)、方策を直接動かして(9–10)、そして安定化の処方箋とともに(11–14)——が、動くコードとして残ったはずである。解説テキストの主張どおり、どのコードも同じ数行(目標値の計算と「(目標 − 推定)×ステップサイズ」)の変奏だったことを、diff を眺めて確かめてほしい。導出の細部に戻りたくなったら [[強化学習の数理_3_演習_手計算編]] へ、理論の全体像は [[強化学習の数理_1_基礎編]] と [[強化学習の数理_2_深層学習編]] へ。
